'use client';

import Link from 'next/link';
import {useMemo} from 'react';
import {usePublicGuideArticles} from '@/components/usePublicGuideArticles';

export function GuideCategoryCmsList({name,terms}:{name:string;terms:string[]}){const items=usePublicGuideArticles();const filtered=useMemo(()=>items.filter(a=>{const hay=`${a.category||''} ${a.title||''} ${a.excerpt||''} ${a.keywords||''}`.toLowerCase();return terms.some(t=>hay.includes(t.toLowerCase()))}),[items,terms]);if(!filtered.length)return null;return <section className="guide-custom-published"><div className="guide-section-head"><div><small>BÀI MỚI TỪ QUẢN TRỊ</small><h2>Bài viết {name} mới xuất bản</h2><p>Các bài đăng mới trong Admin được tự động đưa vào đúng chuyên mục.</p></div><span>{filtered.length} bài mới</span></div><div className="guide-card-grid">{filtered.map(a=>{const href=`/cam-nang/doc?slug=${encodeURIComponent(a.slug)}`;return <article className="guide-pro-card" key={a.id}><Link href={href} className="guide-pro-image" style={{backgroundImage:`url(${a.cover||'https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1000&q=85'})`}} aria-label={a.title}><span>{a.category||name}</span></Link><div className="guide-pro-meta"><small>HappyGo Travel</small><small>{a.date||'Bài mới'}</small><small>{a.readTime||'Cẩm nang'}</small></div><div className="guide-pro-body"><h3><Link href={href}>{a.title}</Link></h3><p>{a.excerpt||'Xem kinh nghiệm và thông tin chi tiết trong bài viết.'}</p><Link className="guide-read" href={href}>Đọc bài viết →</Link></div></article>})}</div></section>}
