'use client';

import Link from 'next/link';
import {useEffect,useMemo,useState} from 'react';

type Article={id:string;title:string;slug:string;category:string;excerpt:string;cover:string;status:string;date:string;readTime?:string;keywords?:string};
const KEY='tn_cms_articles_v4';
export function GuideCategoryCmsList({name,terms}:{name:string;terms:string[]}){const [items,setItems]=useState<Article[]>([]);useEffect(()=>{const load=()=>{try{const raw=JSON.parse(localStorage.getItem(KEY)||'[]') as Article[];setItems(raw.filter(x=>x.status==='published'&&x.title&&x.slug&&!x.id.startsWith('seo_')))}catch{setItems([])}};load();window.addEventListener('tn-articles-updated',load);window.addEventListener('storage',load);return()=>{window.removeEventListener('tn-articles-updated',load);window.removeEventListener('storage',load)}},[]);const filtered=useMemo(()=>items.filter(a=>{const hay=`${a.category||''} ${a.title||''} ${a.excerpt||''} ${a.keywords||''}`.toLowerCase();return terms.some(t=>hay.includes(t.toLowerCase()))}),[items,terms]);if(!filtered.length)return null;return <section className="guide-custom-published"><div className="guide-section-head"><div><small>BÀI MỚI TỪ QUẢN TRỊ</small><h2>Bài viết {name} mới xuất bản</h2><p>Các bài đăng mới trong Admin được tự động đưa vào đúng chuyên mục.</p></div><span>{filtered.length} bài mới</span></div><div className="guide-card-grid">{filtered.map(a=><article className="guide-pro-card" key={a.id}><Link href={`/guide/read?slug=${encodeURIComponent(a.slug)}`} className="guide-pro-image" style={{backgroundImage:`url(${a.cover||'https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1000&q=85'})`}} aria-label={a.title}><span>{a.category||name}</span></Link><div className="guide-pro-meta"><small>HappyGo Travel</small><small>{a.date||'Bài mới'}</small><small>{a.readTime||'Cẩm nang'}</small></div><div className="guide-pro-body"><h3><Link href={`/guide/read?slug=${encodeURIComponent(a.slug)}`}>{a.title}</Link></h3><p>{a.excerpt||'Xem kinh nghiệm và thông tin chi tiết trong bài viết.'}</p><Link className="guide-read" href={`/guide/read?slug=${encodeURIComponent(a.slug)}`}>Đọc bài viết →</Link></div></article>)}</div></section>}
