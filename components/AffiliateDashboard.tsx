'use client';
import {useEffect,useMemo,useState} from 'react';
import {useRouter} from 'next/navigation';

type Villa={id:string;slug:string;name:string;place:string;cover:string;publicPrice:number;affiliateLink:string};
type Referral={id:string;bookingCode:string;bookingStatus:string;villaName:string;customerPhone:string;commissionAmount:number;status:string;createdAt:string;creditedAt:string};
type Payout={id:string;amount:number;status:string;payoutDate:string;receiptUrl:string;createdAt:string};
type AffiliateProfile={name:string;email:string;referralCode:string;phone:string;zalo:string;balance:number;totalCommission:number;commissionRate:number;status:string;bankAccount:string;bankName:string;accountHolder:string};
type Dashboard={affiliate:AffiliateProfile;stats:{clicks:number;closedOrders:number};villas:Villa[];referrals:Referral[];payouts:Payout[]};
type ProfileForm={phone:string;zalo:string;bankAccount:string;bankName:string;accountHolder:string};
const money=(v:number)=>new Intl.NumberFormat('vi-VN').format(Math.round(v))+'đ';
const date=(v:string)=>v?new Intl.DateTimeFormat('vi-VN',{dateStyle:'short',timeStyle:'short'}).format(new Date(v)):'—';
const statusLabel=(v:string)=>({pending:'Chờ duyệt',approved:'Đã duyệt',paid:'Đã thanh toán',cancelled:'Đã hủy',new:'Mới',contacting:'Đang tư vấn',confirmed:'Đã xác nhận',completed:'Hoàn tất'} as Record<string,string>)[v]||v;
const emptyProfile:ProfileForm={phone:'',zalo:'',bankAccount:'',bankName:'',accountHolder:''};

export function AffiliateDashboard(){
 const router=useRouter();
 const[data,setData]=useState<Dashboard|null>(null),[msg,setMsg]=useState(''),[q,setQ]=useState(''),[busy,setBusy]=useState(true),[saving,setSaving]=useState(false),[copied,setCopied]=useState(''),[profile,setProfile]=useState<ProfileForm>(emptyProfile);
 async function load(){setBusy(true);try{const r=await fetch('/api/affiliate/dashboard',{cache:'no-store'});if(r.status===401){router.replace('/affiliate');return}const d=await r.json().catch(()=>({}));if(!r.ok){setMsg(d.error||'Không tải được dashboard.');return}setData(d);setProfile({phone:String(d.affiliate?.phone||''),zalo:String(d.affiliate?.zalo||''),bankAccount:String(d.affiliate?.bankAccount||''),bankName:String(d.affiliate?.bankName||''),accountHolder:String(d.affiliate?.accountHolder||'')})}catch{setMsg('Không kết nối được dashboard CTV.')}finally{setBusy(false)}}
 useEffect(()=>{void load()},[]);
 async function logout(){await fetch('/api/affiliate/auth/logout',{method:'POST'}).catch(()=>{});router.replace('/affiliate');router.refresh()}
 async function copy(v:Villa){try{await navigator.clipboard.writeText(v.affiliateLink);setCopied(v.id);setTimeout(()=>setCopied(''),1800)}catch{setMsg('Không thể copy link trên trình duyệt này.')}}
 async function saveProfile(){setSaving(true);setMsg('');try{const r=await fetch('/api/affiliate/dashboard',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(profile)}),d=await r.json().catch(()=>({}));if(!r.ok){setMsg(d.error||'Không cập nhật được thông tin nhận hoa hồng.');return}setMsg('Đã cập nhật thông tin nhận hoa hồng.');await load()}catch{setMsg('Không kết nối được server để cập nhật hồ sơ.')}finally{setSaving(false)}}
 const villas=useMemo(()=>{const n=q.trim().toLowerCase();return(data?.villas||[]).filter(v=>!n||`${v.name} ${v.place}`.toLowerCase().includes(n))},[data,q]);
 if(busy&&!data)return <main className="affiliate-shell"><div className="affiliate-loading">Đang tải Dashboard CTV...</div></main>;
 if(!data)return <main className="affiliate-shell"><div className="affiliate-loading">{msg||'Không có dữ liệu CTV.'}</div></main>;
 const a=data.affiliate;
 return <main className="affiliate-shell"><header className="affiliate-topbar"><div><div className="affiliate-logo small">HG</div><div><small>HAPPYGO TRAVEL · CTV</small><b>{a.name}</b><span>Mã giới thiệu: {a.referralCode}</span></div></div><button onClick={()=>void logout()}>Đăng xuất</button></header><div className="affiliate-content">{msg&&<p className="affiliate-message">{msg}</p>}<section className="affiliate-welcome"><div><small>DASHBOARD CỘNG TÁC VIÊN</small><h1>Hoa hồng rõ ràng, link chia sẻ sẵn sàng</h1><p>Hoa hồng được ghi nhận khi booking hoàn tất. Tỷ lệ hiện tại: <b>{a.commissionRate}%</b>.</p></div><div className="affiliate-security-note">🔒 Dashboard không truy vấn hoặc hiển thị tên/SĐT chủ nhà, địa chỉ cụ thể hay giá net.</div></section><section className="affiliate-kpis"><article><small>SỐ DƯ VÍ</small><strong>{money(a.balance)}</strong><span>Có thể đối soát thanh toán</span></article><article><small>TỔNG HOA HỒNG</small><strong>{money(a.totalCommission)}</strong><span>Đã ghi nhận từ booking hoàn tất</span></article><article><small>LƯỢT CLICK</small><strong>{data.stats.clicks}</strong><span>Click hợp lệ theo ngày</span></article><article><small>ĐƠN CHỐT THÀNH CÔNG</small><strong>{data.stats.closedOrders}</strong><span>Đã duyệt / đã thanh toán</span></article></section><section className="affiliate-panel"><div className="affiliate-panel-head"><div><small>LINK AFFILIATE</small><h2>Chọn villa để copy link</h2><p>Link tự gắn mã CTV và villa. Khách đặt sau khi truy cập link sẽ được ghi nhận referral.</p></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm tên villa / khu vực"/></div><div className="affiliate-villa-grid">{villas.map(v=><article key={v.id}><div className="affiliate-villa-cover">{v.cover?<img src={v.cover} alt={v.name}/>:<span>🏡</span>}</div><div className="affiliate-villa-body"><small>{v.place||'Villa & Resort'}</small><h3>{v.name}</h3><p>Giá công khai từ <b>{v.publicPrice?money(v.publicPrice):'Liên hệ'}</b></p><div className="affiliate-link-box"><input readOnly value={v.affiliateLink}/><button onClick={()=>void copy(v)}>{copied===v.id?'✓ Đã copy':'Copy link'}</button></div></div></article>)}{!villas.length&&<div className="affiliate-empty">Chưa có villa phù hợp.</div>}</div></section><section className="affiliate-panel"><div className="affiliate-panel-head"><div><small>LỊCH SỬ ĐƠN HÀNG</small><h2>Booking & trạng thái duyệt tiền</h2></div></div><div className="affiliate-table-wrap"><table className="affiliate-table"><thead><tr><th>Booking</th><th>Villa</th><th>Khách</th><th>Trạng thái booking</th><th>Hoa hồng</th><th>Duyệt tiền</th><th>Ngày</th></tr></thead><tbody>{data.referrals.map(r=><tr key={r.id}><td><b>{r.bookingCode}</b></td><td>{r.villaName}</td><td>{r.customerPhone||'—'}</td><td><span className={`affiliate-status ${r.bookingStatus}`}>{statusLabel(r.bookingStatus)}</span></td><td><b>{money(r.commissionAmount)}</b></td><td><span className={`affiliate-status ${r.status}`}>{statusLabel(r.status)}</span></td><td>{date(r.createdAt)}</td></tr>)}{!data.referrals.length&&<tr><td colSpan={7}>Chưa có booking affiliate.</td></tr>}</tbody></table></div></section><section className="affiliate-two"><section className="affiliate-panel"><div className="affiliate-panel-head"><div><small>THANH TOÁN</small><h2>Lịch sử chi hoa hồng</h2></div></div><div className="affiliate-table-wrap"><table className="affiliate-table"><thead><tr><th>Ngày</th><th>Số tiền</th><th>Trạng thái</th><th>Biên nhận</th></tr></thead><tbody>{data.payouts.map(p=><tr key={p.id}><td>{date(p.payoutDate||p.createdAt)}</td><td><b>{money(p.amount)}</b></td><td><span className={`affiliate-status ${p.status}`}>{statusLabel(p.status)}</span></td><td>{p.receiptUrl?<a href={p.receiptUrl} target="_blank" rel="noreferrer">Xem biên nhận</a>:'—'}</td></tr>)}{!data.payouts.length&&<tr><td colSpan={4}>Chưa có đợt thanh toán.</td></tr>}</tbody></table></div></section><aside className="affiliate-bank-card affiliate-bank-edit"><small>THÔNG TIN NHẬN HOA HỒNG</small><h3>Tự cập nhật hồ sơ thanh toán</h3><div className="affiliate-bank-form"><label>Số điện thoại<input value={profile.phone} onChange={e=>setProfile({...profile,phone:e.target.value})} placeholder="09xx xxx xxx"/></label><label>Zalo<input value={profile.zalo} onChange={e=>setProfile({...profile,zalo:e.target.value})} placeholder="SĐT / tên Zalo"/></label><label>Ngân hàng<input value={profile.bankName} onChange={e=>setProfile({...profile,bankName:e.target.value})} placeholder="VD: Vietcombank"/></label><label>Số tài khoản<input value={profile.bankAccount} onChange={e=>setProfile({...profile,bankAccount:e.target.value})} placeholder="Số tài khoản"/></label><label>Chủ tài khoản<input value={profile.accountHolder} onChange={e=>setProfile({...profile,accountHolder:e.target.value})} placeholder="NGUYEN VAN A"/></label><button type="button" disabled={saving} onClick={()=>void saveProfile()}>{saving?'Đang lưu...':'Lưu thông tin nhận tiền'}</button></div><span>Thông tin này chỉ dùng cho đối soát/chi hoa hồng. CTV chỉ sửa hồ sơ của chính mình.</span></aside></section></div></main>
}
