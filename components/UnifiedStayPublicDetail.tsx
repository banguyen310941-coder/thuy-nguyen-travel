'use client';

import Link from 'next/link';
import {Suspense} from 'react';
import {BookingInquiry} from '@/components/BookingInquiry';
import {PublishedUnits} from '@/components/PublishedUnits';
import {ContactPhoneInline} from '@/components/ContactPhoneInline';
import {SafeBackground,travelFallback} from '@/components/SafeImage';

type Unit={id:string;code:string;name:string;bedrooms?:string;beds?:string;capacity:string;area:string;view:string;meal:string;weekdayPrice:string;weekendPrice:string;holidayPrice:string;extraAdult?:string;extraChild?:string;status:string;amenities?:string;images?:string};
type StayProduct={slug:string;type:string;name:string;place:string;address:string;price:string;summary:string;cover:string;gallery:string;rating:string;category:string;checkin:string;checkout:string;amenities:string;policies:string;childrenPolicy:string;extraCharge:string;content:string;units:Unit[]};
const lines=(v?:string)=>String(v||'').split(/\n+/).map(x=>x.trim()).filter(Boolean);
const money=(v?:string)=>{const n=Number(String(v||'').replace(/\D/g,''));return n||0};
const fmt=(n:number)=>new Intl.NumberFormat('vi-VN').format(n)+'đ';
export function UnifiedStayPublicDetail({product}:{product:StayProduct}){
 const isVilla=product.type==='Villa & Resort';const fallback=travelFallback(product.type);const gallery=[product.cover,...lines(product.gallery)].filter(Boolean);while(gallery.length<3&&gallery[0])gallery.push(gallery[0]);const highlights=lines(product.amenities);const policies=lines(product.policies);const unitPrices=(product.units||[]).flatMap(u=>[money(u.weekdayPrice),money(u.weekendPrice),money(u.holidayPrice)]).filter(Boolean);const displayPrice=unitPrices.length?`Từ ${fmt(Math.min(...unitPrices))}`:product.price||'Liên hệ giá';
 return <div className="product-detail-v2"><section className="pd-head"><div className="container"><div className="pd-breadcrumb"><Link href="/">Trang chủ</Link><span>›</span><Link href="/stay">Lưu trú</Link><span>›</span><b>{product.name}</b></div><div className="pd-title"><div><span className="pd-type">{isVilla?'Villa & Resort':'Khách sạn'}</span><h1>{product.name}</h1><p>📍 {product.address||product.place||'Đang cập nhật địa điểm'}</p></div><div className="pd-rating"><span>{product.rating?'Rất tốt':'HappyGo đề xuất'}</span><b>{product.rating||'✓'}</b></div></div></div></section><section className="container pd-gallery"><SafeBackground src={gallery[0]} fallback={fallback} className="pd-main-img" ariaLabel={product.name}/><SafeBackground src={gallery[1]||gallery[0]} fallback={fallback} ariaLabel={`${product.name} 2`}/><SafeBackground src={gallery[2]||gallery[0]} fallback={fallback} ariaLabel={`${product.name} 3`}/><a className="pd-gallery-open" href={gallery[0]||fallback} target="_blank" rel="noreferrer">📷 Xem ảnh lớn</a></section><section className="container pd-summary-grid"><div className="pd-summary-card"><span className="sub-kicker">{product.category||product.type}</span><h2>{isVilla?'Villa nghỉ dưỡng phù hợp gia đình & nhóm':'Lưu trú nghỉ dưỡng tiện nghi'}</h2><p>{product.summary||'Thông tin lưu trú đang được cập nhật.'}</p>{product.content&&<p className="cms-preline">{product.content}</p>}<div className="pd-quick-info"><span>✓ {isVilla?'Nhiều căn villa quản lý riêng':'Nhiều hạng phòng quản lý riêng'}</span><span>✓ Giá ngày thường / cuối tuần / lễ</span><span>✓ Ảnh, sức chứa & tiện ích riêng từng căn</span><span>✓ Xác nhận tình trạng theo ngày</span></div></div><div className="pd-price-card"><small>GIÁ TỐT NHẤT THEO NGÀY</small><strong>{displayPrice}</strong><p>Giá thay đổi theo căn/phòng, ngày ở và số khách.</p><a href="#booking">Kiểm tra giá & phòng trống</a><ContactPhoneInline/></div></section><nav className="pd-tabs"><div className="container"><a href="#overview">Tổng quan</a><a href="#units">{isVilla?'Từng căn Villa':'Từng hạng phòng'}</a><a href="#amenities">Tiện ích</a><a href="#policy">Chính sách</a><a href="#reviews">Đánh giá</a></div></nav><section className="container pd-body"><main><section id="overview" className="pd-block"><h2>Tổng quan</h2><p>{product.summary||'Thông tin lưu trú đang được cập nhật.'}</p>{highlights.length>0&&<div className="pd-highlight-grid">{highlights.map(item=><div key={item}>✓ <b>{item}</b></div>)}</div>}</section><Suspense fallback={<section className="detail-block live-units"><div className="live-units-head"><h2>Đang tải căn / hạng phòng...</h2></div></section>}><PublishedUnits slug={product.slug} providedUnits={product.units||[]} label={isVilla?'Danh sách từng căn Villa & giá':'Danh sách hạng phòng & giá'}/></Suspense><section id="amenities" className="pd-block"><h2>Tiện ích nổi bật</h2>{highlights.length?<div className="pd-highlight-grid">{highlights.map(item=><div key={item}>✦ <b>{item}</b></div>)}</div>:<p>Các tiện ích chi tiết được cập nhật theo từng căn / hạng phòng.</p>}</section><section id="policy" className="pd-block"><h2>Chính sách lưu trú</h2><div className="pd-policy-grid"><div><b>Nhận / trả phòng</b><p>{product.checkin||product.checkout?`${product.checkin||'—'} / ${product.checkout||'—'}`:'Giờ nhận và trả phòng được xác nhận theo từng hạng phòng.'}</p></div><div><b>Trẻ em</b><p>{product.childrenPolicy||'Phụ thu tùy độ tuổi, hạng phòng và số khách thực tế.'}</p></div><div><b>Phụ thu</b><p>{product.extraCharge||'Mức phụ thu được xác nhận theo số khách và từng hạng phòng.'}</p></div><div><b>Hoàn hủy</b><p>{policies.length?policies.join(' · '):'Áp dụng theo gói giá và thời điểm đặt.'}</p></div></div></section><section id="reviews" className="pd-block"><h2>Đánh giá</h2><div className="pd-review-summary"><b>{product.rating?`${product.rating}/10`:'✓'}</b><div><strong>{product.rating?'Rất tốt':'Sản phẩm đã được duyệt'}</strong><p>Thông tin và chất lượng sản phẩm được HappyGo kiểm duyệt trước khi mở bán.</p></div></div></section></main><aside id="booking"><BookingInquiry product={product.name} kind={isVilla?'villa / resort':'khách sạn'}/></aside></section></div>;
}
