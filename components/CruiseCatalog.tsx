'use client';

import Link from 'next/link';
import {useSearchParams} from 'next/navigation';
import {useEffect,useMemo,useState} from 'react';
import {cruises} from '@/data/catalog';
import {useSiteSettings} from '@/components/useSiteSettings';
import {SafeBackground,travelFallback} from '@/components/SafeImage';
import {productVisual} from '@/components/ProductVisuals';

type CmsProduct={id:string;type:string;name:string;slug:string;place:string;duration:string;price:string;status:string;summary:string;cover:string;rating:string};

export function CruiseCatalog(){const params=useSearchParams();const settings=useSiteSettings();const q=(params.get('q')||'').toLowerCase();const durationParam=(params.get('duration')||'').toLowerCase();const[bay,setBay]=useState('all');const[cms,setCms]=useState<CmsProduct[]>([]);useEffect(()=>{const load=()=>{try{setCms(JSON.parse(localStorage.getItem('tn_cms_products_v3_units')||'[]') as CmsProduct[])}catch{setCms([])}};load();window.addEventListener('tn-products-updated',load);window.addEventListener('storage',load);return()=>{window.removeEventListener('tn-products-updated',load);window.removeEventListener('storage',load)}},[]);const cruiseCms=useMemo(()=>cms.filter(x=>x.type==='Du thuyền'),[cms]);const durationMatch=(duration:string)=>{const d=(duration||'').toLowerCase();return !durationParam||(durationParam==='day'&&(/trong ngày|tiếng/.test(d)))||(durationParam==='2n1d'&&(/2 ngày 1 đêm|2n1đ|2n1d/.test(d)))||(durationParam==='3n2d'&&(/3 ngày 2 đêm|3n2đ|3n2d/.test(d)))};const bayMatch=(place:string)=>bay==='all'||(bay==='halong'&&(place||'').toLowerCase().includes('hạ long'))||(bay==='lanha'&&(place||'').toLowerCase().includes('lan hạ'));const staticSlugs=useMemo(()=>new Set(cruises.map(x=>x.slug)),[]);const merged=useMemo(()=>cruises.flatMap(c=>{const a=cruiseCms.find(x=>x.slug===c.slug);if(a&&a.status!=='published')return[];return [a?{...c,name:a.name||c.name,bay:a.place||c.bay,duration:a.duration||c.duration,summary:a.summary||c.summary,image:a.cover||c.image,priceFrom:a.price||c.priceFrom}:c]}),[cruiseCms]);const cmsOnly=useMemo(()=>cruiseCms.filter(x=>x.status==='published'&&x.slug&&!staticSlugs.has(x.slug)),[cruiseCms,staticSlugs]);const filtered=useMemo(()=>merged.filter(item=>(!q||`${item.name} ${item.bay} ${item.duration} ${item.summary}`.toLowerCase().includes(q))&&durationMatch(item.duration)&&bayMatch(item.bay)),[merged,q,durationParam,bay]);const cmsFiltered=useMemo(()=>cmsOnly.filter(item=>(!q||`${item.name} ${item.place} ${item.duration} ${item.summary}`.toLowerCase().includes(q))&&durationMatch(item.duration)&&bayMatch(item.place)),[cmsOnly,q,durationParam,bay]);const total=filtered.length+cmsFiltered.length;const zalo=settings.zalo.replace(/\D/g,'')||settings.hotline.replace(/\D/g,'');return <><div className="sub-toolbar"><div><b>{total} hành trình phù hợp</b><br/><span>{q?`Vịnh/từ khóa: ${params.get('q')}`:'Trong ngày · 2N1Đ · Nghỉ đêm'}{durationParam?' · Thời lượng đã chọn':''}</span></div><div className="sub-filter-row"><button type="button" className={`sub-chip ${bay==='all'?'active':''}`} onClick={()=>setBay('all')}>Tất cả</button><button type="button" className={`sub-chip ${bay==='halong'?'active':''}`} onClick={()=>setBay('halong')}>Hạ Long</button><button type="button" className={`sub-chip ${bay==='lanha'?'active':''}`} onClick={()=>setBay('lanha')}>Lan Hạ</button></div></div><div className="catalog-grid">{filtered.map(item=><article className="catalog-card" key={item.slug}><SafeBackground src={productVisual(item.slug,item.image)} fallback={travelFallback('cruise')} className="catalog-image" ariaLabel={item.name}/><div className="catalog-body"><small>{item.bay} · {item.duration}</small><h3>{item.name}</h3><p>{item.summary}</p>{item.priceFrom&&<p><b style={{color:'#f15a24'}}>Từ {item.priceFrom}</b></p>}<div className="catalog-actions"><Link className="main" href={`/cruises/${item.slug}`}>Xem chi tiết</Link><a className="secondary" href={`https://zalo.me/${zalo}`} target="_blank" rel="noreferrer">Zalo →</a></div></div></article>)}{cmsFiltered.map(item=><article className="catalog-card cms-public-card" key={item.id}><SafeBackground src={item.cover} fallback={travelFallback('cruise')} className="catalog-image" ariaLabel={item.name}/><div className="catalog-body"><small>{item.place||'Du thuyền'} · {item.duration||'Lịch trình linh hoạt'}</small><h3>{item.name}</h3><p>{item.summary||'Thông tin du thuyền được xuất bản từ hệ thống quản trị.'}</p><p><b style={{color:'#f15a24'}}>Từ {item.price||'Liên hệ'}</b></p><div className="catalog-actions"><Link className="main" href={`/product?slug=${encodeURIComponent(item.slug)}`}>Xem chi tiết</Link><a className="secondary" href={`https://zalo.me/${zalo}`} target="_blank" rel="noreferrer">Zalo →</a></div></div></article>)}</div>{!total&&<div className="empty-results"><b>Chưa tìm thấy du thuyền phù hợp</b><p>Thử đổi vịnh, thời lượng hoặc ngày đi.</p><a href={`https://zalo.me/${zalo}`} target="_blank" rel="noreferrer">Chat Zalo để được tìm giúp</a></div>}</>}
