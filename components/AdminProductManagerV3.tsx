'use client';

import {useEffect,useMemo,useState} from 'react';
import {AdminMediaPicker} from '@/components/AdminMediaLibrary';
type ProductType='Sản phẩm'|'Villa & Resort'|'Khách sạn'|'Du thuyền';type Status='draft'|'published'|'hidden';type UnitStatus='available'|'hold'|'soldout'|'hidden';
type Unit={id:string;code:string;name:string;bedrooms:string;beds:string;capacity:string;area:string;view:string;meal:string;amenities:string;weekdayPrice:string;weekendPrice:string;holidayPrice:string;extraAdult:string;extraChild:string;images:string;status:UnitStatus;note:string};
type Product={id:string;type:ProductType;name:string;slug:string;place:string;price:string;status:Status;summary:string;cover:string;gallery:string;rating:string;category:string;address:string;checkin:string;checkout:string;amenities:string;policies:string;childrenPolicy:string;extraCharge:string;duration:string;pickup:string;boarding:string;itinerary:string;content:string;seoTitle:string;seoDescription:string;units:Unit[];updatedAt:string};
const KEY='tn_cms_products_v3_units';const uid=(p='id')=>`${p}_${Date.now()}_${Math.random().toString(36).slice(2,7)}`;const slugify=(v:string)=>v.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');
const blankUnit=():Unit=>({id:uid('u'),code:'',name:'',bedrooms:'',beds:'',capacity:'',area:'',view:'',meal:'',amenities:'',weekdayPrice:'',weekendPrice:'',holidayPrice:'',extraAdult:'',extraChild:'',images:'',status:'available',note:''});
const blank=(type:ProductType):Product=>({id:'',type,name:'',slug:'',place:'',price:'',status:'draft',summary:'',cover:'',gallery:'',rating:'',category:'',address:'',checkin:'14:00',checkout:'12:00',amenities:'',policies:'',childrenPolicy:'',extraCharge:'',duration:'',pickup:'',boarding:'',itinerary:'',content:'',seoTitle:'',seoDescription:'',units:[],updatedAt:''});
const seeds:Product[]=[{...blank('Villa & Resort'),id:'stay_oceanami',name:'Oceanami Villas & Beach Club',slug:'oceanami-villas-beach-club',place:'Phước Hải, Bà Rịa - Vũng Tàu',price:'4.500.000đ',status:'published',rating:'8.8',summary:'Khu nghỉ dưỡng 5 sao với nhiều căn villa riêng biệt.',units:[{...blankUnit(),id:'oca_a12',code:'OCA-A12',name:'Villa 3PN A12',bedrooms:'3',capacity:'6 người lớn + 2 trẻ em',area:'350 m²',view:'Vườn',weekdayPrice:'4.500.000đ',weekendPrice:'5.500.000đ'}]},{...blank('Khách sạn'),id:'stay_vinpearl_nt',name:'Vinpearl Resort Nha Trang',slug:'vinpearl-resort-nha-trang',place:'Hòn Tre, Nha Trang',status:'published',summary:'Resort biển phù hợp gia đình và cặp đôi.'},{...blank('Du thuyền'),id:'cruise_ambassador1',name:'Ambassador I - Vịnh Hạ Long',slug:'ambassador-i-ha-long-2n1d',place:'Vịnh Hạ Long',duration:'2 ngày 1 đêm',price:'4.500.000đ/khách',status:'published'}];
function F({label,children,wide=false}:{label:string;children:React.ReactNode;wide?:boolean}){return <label className={wide?'pm-field wide':'pm-field'}><span>{label}</span>{children}</label>}
export function AdminProductManagerV3({type}:{type:ProductType}){const [items,setItems]=useState<Product[]>([]);const [mode,setMode]=useState<'list'|'edit'>('list');const [form,setForm]=useState<Product>(blank(type));const [q,setQ]=useState('');const [msg,setMsg]=useState('');const [unitOpen,setUnitOpen]=useState<string|null>(null);
 useEffect(()=>{try{const raw=localStorage.getItem(KEY);setItems(raw?JSON.parse(raw):seeds)}catch{setItems(seeds)}},[]);useEffect(()=>{setMode('list');setForm(blank(type));setMsg('');setUnitOpen(null)},[type]);
 const saveItems=(next:Product[])=>{try{localStorage.setItem(KEY,JSON.stringify(next));setItems(next);window.dispatchEvent(new Event('tn-products-updated'));return true}catch{setMsg('Chưa lưu được sản phẩm. Bộ nhớ trình duyệt có thể đã đầy; dữ liệu đang nhập vẫn được giữ trên màn hình.');return false}};const shown=useMemo(()=>items.filter(p=>(type==='Sản phẩm'||p.type===type)&&(!q||`${p.name} ${p.place} ${p.category}`.toLowerCase().includes(q.toLowerCase()))),[items,type,q]);const set=<K extends keyof Product>(k:K,v:Product[K])=>setForm(s=>({...s,[k]:v}));const setUnit=<K extends keyof Unit>(id:string,k:K,v:Unit[K])=>setForm(s=>({...s,units:s.units.map(u=>u.id===id?{...u,[k]:v}:u)}));
 const add=()=>{setForm(blank(type==='Sản phẩm'?'Villa & Resort':type));setMode('edit')};const edit=(p:Product)=>{setForm(JSON.parse(JSON.stringify(p)));setMode('edit');setUnitOpen(null);setMsg('')};const duplicate=(p:Product)=>{const copy={...JSON.parse(JSON.stringify(p)),id:uid('p'),name:`${p.name} - Bản sao`,slug:`${p.slug}-copy`,status:'draft' as Status,units:(p.units||[]).map((u:Unit)=>({...u,id:uid('u')}))};if(saveItems([...items,copy]))edit(copy)};const remove=(p:Product)=>{const n=(p.units||[]).length;if(!confirm(`Xóa “${p.name}”${n?` và ${n} căn/phòng/cabin`:''}?`))return;if(saveItems(items.filter(x=>x.id!==p.id))&&form.id===p.id)setMode('list')};const save=(status:Status)=>{if(!form.name.trim()){setMsg('Vui lòng nhập tên sản phẩm.');return}const next={...form,id:form.id||uid('p'),slug:form.slug||slugify(form.name),status,updatedAt:new Date().toISOString()};const nextItems=items.some(x=>x.id===next.id)?items.map(x=>x.id===next.id?next:x):[...items,next];if(!saveItems(nextItems))return;setForm(next);setMsg(status==='published'?'Đã cập nhật và đăng sản phẩm.':'Đã lưu bản nháp sản phẩm.')};
 const addUnit=()=>{const u=blankUnit();setForm(s=>({...s,units:[...(s.units||[]),u]}));setUnitOpen(u.id)};const duplicateUnit=(u:Unit)=>{const x={...u,id:uid('u'),code:u.code?`${u.code}-COPY`:'',name:`${u.name||'Hạng phòng'} - Bản sao`};setForm(s=>({...s,units:[...s.units,x]}));setUnitOpen(x.id)};const removeUnit=(u:Unit)=>{if(!confirm(`Xóa ${u.name||u.code||'căn/phòng/cabin này'}?`))return;setForm(s=>({...s,units:s.units.filter(x=>x.id!==u.id)}));setUnitOpen(null)};
 if(mode==='list')return <section className="admin-panel product-manager"><div className="admin-panel-head"><div><h2>{type}</h2><p>Thêm, sửa, nhân bản, xóa và quản lý ảnh trực tiếp.</p></div><button type="button" className="admin-primary" onClick={add}>+ Thêm mới</button></div><div className="admin-list-toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm sản phẩm..."/><span>{shown.length} sản phẩm</span></div><div className="pm-list">{shown.map(p=><article key={p.id}>{p.cover?<img src={p.cover} alt={p.name}/>:<div className="pm-placeholder">{p.type==='Du thuyền'?'🛳':p.type==='Khách sạn'?'🏨':'🏝'}</div>}<div className="pm-info"><b>{p.name}</b><span>{p.place||'Chưa nhập địa điểm'}</span><small>{(p.units||[]).length} căn / phòng / cabin</small></div><strong>{p.price||'Liên hệ'}</strong><em className={`pm-status ${p.status}`}>{p.status==='published'?'Đang bán':p.status==='hidden'?'Đang ẩn':'Bản nháp'}</em><div className="pm-actions"><button type="button" onClick={()=>edit(p)}>Sửa</button><button type="button" onClick={()=>duplicate(p)}>Nhân bản</button><button type="button" className="danger-action" onClick={()=>remove(p)}>Xóa</button></div></article>)}</div></section>;
 return <section className="admin-editor-shell product-manager-editor"><div className="admin-editor-main"><div className="editor-title-row"><button type="button" className="editor-back" onClick={()=>setMode('list')}>← Danh sách</button><b>{form.id?'Chỉnh sửa sản phẩm':'Thêm sản phẩm mới'}</b><span>{msg}</span></div><input className="post-title-input" value={form.name} onChange={e=>{const old=form.name;set('name',e.target.value);if(!form.slug||form.slug===slugify(old))set('slug',slugify(e.target.value))}} placeholder="Tên khu / sản phẩm"/><div className="slug-row"><span>Đường dẫn:</span><input value={form.slug} onChange={e=>set('slug',slugify(e.target.value))}/></div>
 <section className="pm-section"><h3>1. Thông tin chung</h3><div className="pm-grid"><F label="Loại"><select value={form.type} onChange={e=>set('type',e.target.value as ProductType)}><option>Villa & Resort</option><option>Khách sạn</option><option>Du thuyền</option></select></F><F label="Phân loại"><input value={form.category} onChange={e=>set('category',e.target.value)}/></F><F label="Khu vực"><input value={form.place} onChange={e=>set('place',e.target.value)}/></F><F label="Địa chỉ"><input value={form.address} onChange={e=>set('address',e.target.value)}/></F><F label="Giá từ"><input value={form.price} onChange={e=>set('price',e.target.value)}/></F><F label="Điểm đánh giá"><input value={form.rating} onChange={e=>set('rating',e.target.value)}/></F><F label="Check-in"><input value={form.checkin} onChange={e=>set('checkin',e.target.value)}/></F><F label="Check-out"><input value={form.checkout} onChange={e=>set('checkout',e.target.value)}/></F><F label="Thời lượng / hành trình"><input value={form.duration} onChange={e=>set('duration',e.target.value)}/></F><F label="Điểm đón / bến"><input value={form.pickup} onChange={e=>set('pickup',e.target.value)}/></F><F label="Mô tả ngắn" wide><textarea rows={4} value={form.summary} onChange={e=>set('summary',e.target.value)}/></F></div></section>
 <section className="pm-section"><h3>2. Ảnh đại diện & Album</h3><p className="pm-section-note">Chọn ảnh đã có trong thư viện hoặc tải ảnh mới từ máy/điện thoại.</p><div className="media-product-grid"><div><b>Ảnh đại diện</b><AdminMediaPicker value={form.cover} onChange={v=>set('cover',v)} label="Chọn ảnh đại diện"/></div><div><b>Album sản phẩm</b><AdminMediaPicker multiple value={form.gallery} onChange={v=>set('gallery',v)} label="Chọn ảnh Album"/></div></div></section>
 <section className="pm-section unit-section"><div className="unit-section-head"><div><h3>3. Căn / Hạng phòng / Cabin & giá</h3><p>Mỗi căn/phòng/cabin có ảnh và giá riêng.</p></div><button type="button" className="admin-primary" onClick={addUnit}>+ Thêm căn / phòng</button></div>{!(form.units||[]).length&&<div className="unit-empty">Chưa có căn/phòng/cabin nào.</div>}<div className="unit-list">{(form.units||[]).map((u,i)=><article className={unitOpen===u.id?'unit-card open':'unit-card'} key={u.id}><button type="button" className="unit-summary" onClick={()=>setUnitOpen(unitOpen===u.id?null:u.id)}><span className="unit-index">{i+1}</span><div><b>{u.name||'Chưa đặt tên'}</b><small>{u.code||'Chưa có mã'} · {u.capacity||'Chưa nhập sức chứa'}</small></div><strong>{u.weekendPrice||u.weekdayPrice||'Chưa có giá'}</strong><em>{u.status==='available'?'Đang bán':u.status==='hold'?'Tạm giữ':u.status==='soldout'?'Hết':'Ẩn'}</em></button>{unitOpen===u.id&&<div className="unit-form"><div className="pm-grid"><F label="Mã căn/phòng"><input value={u.code} onChange={e=>setUnit(u.id,'code',e.target.value)}/></F><F label="Tên căn / hạng"><input value={u.name} onChange={e=>setUnit(u.id,'name',e.target.value)}/></F><F label="Số phòng ngủ"><input value={u.bedrooms} onChange={e=>setUnit(u.id,'bedrooms',e.target.value)}/></F><F label="Loại giường"><input value={u.beds} onChange={e=>setUnit(u.id,'beds',e.target.value)}/></F><F label="Sức chứa"><input value={u.capacity} onChange={e=>setUnit(u.id,'capacity',e.target.value)}/></F><F label="Diện tích"><input value={u.area} onChange={e=>setUnit(u.id,'area',e.target.value)}/></F><F label="View"><input value={u.view} onChange={e=>setUnit(u.id,'view',e.target.value)}/></F><F label="Bữa ăn"><input value={u.meal} onChange={e=>setUnit(u.id,'meal',e.target.value)}/></F><F label="Giá ngày thường"><input value={u.weekdayPrice} onChange={e=>setUnit(u.id,'weekdayPrice',e.target.value)}/></F><F label="Giá cuối tuần"><input value={u.weekendPrice} onChange={e=>setUnit(u.id,'weekendPrice',e.target.value)}/></F><F label="Giá lễ/Tết"><input value={u.holidayPrice} onChange={e=>setUnit(u.id,'holidayPrice',e.target.value)}/></F><F label="Phụ thu người lớn"><input value={u.extraAdult} onChange={e=>setUnit(u.id,'extraAdult',e.target.value)}/></F><F label="Phụ thu trẻ em"><input value={u.extraChild} onChange={e=>setUnit(u.id,'extraChild',e.target.value)}/></F><F label="Trạng thái"><select value={u.status} onChange={e=>setUnit(u.id,'status',e.target.value as UnitStatus)}><option value="available">Còn bán</option><option value="hold">Tạm giữ</option><option value="soldout">Hết phòng</option><option value="hidden">Ẩn</option></select></F><div className="wide"><b className="field-label">Album riêng của căn/phòng/cabin</b><AdminMediaPicker multiple value={u.images} onChange={v=>setUnit(u.id,'images',v)} label="Chọn ảnh căn/phòng"/></div><F label="Tiện ích riêng" wide><textarea rows={3} value={u.amenities} onChange={e=>setUnit(u.id,'amenities',e.target.value)}/></F><F label="Ghi chú nội bộ" wide><textarea rows={3} value={u.note} onChange={e=>setUnit(u.id,'note',e.target.value)}/></F></div><div className="unit-actions"><button type="button" onClick={()=>duplicateUnit(u)}>Nhân bản căn</button><button type="button" className="danger-action" onClick={()=>removeUnit(u)}>Xóa căn / phòng</button></div></div>}</article>)}</div></section>
 <section className="pm-section"><h3>4. Nội dung & chính sách</h3><div className="pm-grid"><F label="Tiện ích" wide><textarea rows={5} value={form.amenities} onChange={e=>set('amenities',e.target.value)}/></F><F label="Chính sách" wide><textarea rows={6} value={form.policies} onChange={e=>set('policies',e.target.value)}/></F><F label="Chính sách trẻ em" wide><textarea rows={4} value={form.childrenPolicy} onChange={e=>set('childrenPolicy',e.target.value)}/></F><F label="Nội dung chi tiết" wide><textarea rows={8} value={form.content} onChange={e=>set('content',e.target.value)}/></F><F label="SEO title" wide><input value={form.seoTitle} onChange={e=>set('seoTitle',e.target.value)}/></F><F label="Meta description" wide><textarea rows={4} value={form.seoDescription} onChange={e=>set('seoDescription',e.target.value)}/></F></div></section></div><aside className="admin-editor-side"><div className="editor-box publish-box"><h3>Xuất bản</h3><label>Trạng thái<select value={form.status} onChange={e=>set('status',e.target.value as Status)}><option value="published">Đang bán</option><option value="draft">Bản nháp</option><option value="hidden">Ẩn</option></select></label><div className="publish-actions"><button type="button" onClick={()=>save('draft')}>Lưu nháp</button><button type="button" className="admin-primary" onClick={()=>save('published')}>{form.id?'Cập nhật & đăng':'Thêm & đăng'}</button></div>{form.id&&<button type="button" className="danger-action full-danger" onClick={()=>remove(form)}>Xóa toàn bộ sản phẩm</button>}</div><div className="editor-box"><h3>Ảnh đại diện</h3>{form.cover?<img className="admin-cover-preview" src={form.cover} alt=""/>:<small>Chưa chọn ảnh</small>}</div></aside></section>}
