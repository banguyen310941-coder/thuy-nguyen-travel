'use client';

import { useEffect, useState } from 'react';

type Customer={id:number;customer_name:string;phone:string;email?:string|null;status:'lead'|'contacting'|'customer'|'inactive';note?:string|null;booking_count:number;latest_booking_at?:string|null;};
type Booking={id:number;code:string;product:string;status:string;created_at:string;start_date?:string|null;end_date?:string|null;};
const API_BASE=process.env.NEXT_PUBLIC_API_BASE_URL||'';
const labels:Record<Customer['status'],string>={lead:'Khách tiềm năng',contacting:'Đang tư vấn',customer:'Đã mua',inactive:'Ngừng theo dõi'};

export function AdminCustomers(){
  const [key,setKey]=useState('');const [items,setItems]=useState<Customer[]>([]);const [q,setQ]=useState('');const [status,setStatus]=useState('');const [error,setError]=useState('');const [selected,setSelected]=useState<Customer|null>(null);const [history,setHistory]=useState<Booking[]>([]);
  useEffect(()=>setKey(localStorage.getItem('tn_admin_api_key')||''),[]);
  async function load(){if(!API_BASE){setError('Chưa cấu hình backend. CRM thật sẽ hoạt động khi có hosting và NEXT_PUBLIC_API_BASE_URL.');return;}if(!key){setError('Nhập khóa quản trị API trước.');return;}setError('');const p=new URLSearchParams();if(q)p.set('q',q);if(status)p.set('status',status);const r=await fetch(`${API_BASE.replace(/\/$/,'')}/api/customers?${p}`,{headers:{'x-admin-key':key}});if(!r.ok){setError(r.status===401?'Khóa quản trị không đúng.':'Không tải được CRM.');return;}const d=await r.json();setItems(d.items||[]);localStorage.setItem('tn_admin_api_key',key);}
  async function openCustomer(c:Customer){setSelected(c);if(!API_BASE||!key)return;const r=await fetch(`${API_BASE.replace(/\/$/,'')}/api/customers/${c.id}`,{headers:{'x-admin-key':key}});if(r.ok){const d=await r.json();setHistory(d.bookings||[]);}}
  async function updateStatus(c:Customer,next:Customer['status']){if(!API_BASE||!key)return;const r=await fetch(`${API_BASE.replace(/\/$/,'')}/api/customers/${c.id}`,{method:'PATCH',headers:{'Content-Type':'application/json','x-admin-key':key},body:JSON.stringify({status:next})});if(r.ok){setItems(v=>v.map(i=>i.id===c.id?{...i,status:next}:i));if(selected?.id===c.id)setSelected({...selected,status:next});}}
  return <section className="admin-panel"><div className="admin-panel-head"><div><h2>Khách hàng / CRM</h2><p>Mỗi số điện thoại là một hồ sơ khách; nhiều lần đặt sẽ gom vào cùng lịch sử.</p></div></div><div className="admin-form-row"><label>Khóa quản trị API<input type="password" value={key} onChange={e=>setKey(e.target.value)} placeholder="ADMIN_API_KEY"/></label><label>Tìm khách<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tên, số điện thoại, email"/></label></div><div className="admin-form-row"><label>Trạng thái<select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Tất cả</option><option value="lead">Khách tiềm năng</option><option value="contacting">Đang tư vấn</option><option value="customer">Đã mua</option><option value="inactive">Ngừng theo dõi</option></select></label><div className="admin-load-wrap"><button className="admin-primary" onClick={load}>Tải CRM</button></div></div>{error&&<p className="admin-api-note">{error}</p>}<div className="admin-customer-grid">{items.map(c=><article className="admin-customer-card" key={c.id}><div><b>{c.customer_name}</b><a href={`tel:${c.phone}`}>{c.phone}</a><small>{c.email||'Không có email'}</small></div><div><span>{Number(c.booking_count||0)} đơn</span><small>{c.latest_booking_at?new Date(c.latest_booking_at).toLocaleDateString('vi-VN'):'Chưa có lịch sử'}</small></div><select value={c.status} onChange={e=>updateStatus(c,e.target.value as Customer['status'])}><option value="lead">Khách tiềm năng</option><option value="contacting">Đang tư vấn</option><option value="customer">Đã mua</option><option value="inactive">Ngừng theo dõi</option></select><button onClick={()=>openCustomer(c)}>Xem lịch sử</button></article>)}</div>{selected&&<div className="admin-customer-detail"><div className="admin-panel-head"><div><h3>{selected.customer_name}</h3><p>{selected.phone} · {labels[selected.status]}</p></div><button onClick={()=>setSelected(null)}>Đóng</button></div><div className="admin-table">{history.length?history.map(b=><div key={b.id}><b>{b.code}</b><span>{b.product}</span><span>{b.start_date||'—'} → {b.end_date||'—'}</span><em>{b.status}</em></div>):<p>Chưa có lịch sử đơn.</p>}</div></div>}</section>;
}
