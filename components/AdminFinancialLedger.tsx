'use client';
import {useEffect,useMemo,useState} from 'react';import {bookingRevenue,customerKey,isOwner,money,readAssignments,readCurrentStaff,type AdminStaff} from '@/components/AdminSalesAccess';
type Booking={id:number;code:string;product:string;customer_name:string;phone:string;email?:string|null;status:string;created_at:string;start_date?:string|null;salesStaffId?:string;salesStaffName?:string;revenue?:number;costPrice?:number};type Payment={id:string;bookingId:number;bookingCode:string;customerName:string;supplierName:string;amount:number;customerPaidAmount:number;customerPaidType:'deposit'|'full';status:'draft'|'pending_director'|'changes_requested'|'rejected'|'approved'|'paid';createdById:string;createdByName:string;createdAt:string;submittedAt?:string;paidAmount?:number;paidAt?:string;paidBy?:string};const BOOKINGS='tn_local_bookings_v1',PAYMENTS='happygo_payment_requests_v1';function read<T>(k:string,f:T):T{try{const x=JSON.parse(localStorage.getItem(k)||'');return x??f}catch{return f}}function canSee(staff:AdminStaff,b:Booking){if(isOwner(staff)||staff.role==='accounting'||staff.department==='accounting')return true;const a=readAssignments()[customerKey(b.phone,b.email)];return b.salesStaffId===staff.id||a?.staffId===staff.id}function num(v:any){const n=Number(v||0);return Number.isFinite(n)?n:0}
export function AdminFinancialLedger(){const[staff,setStaff]=useState<AdminStaff|null>(null),[bookings,setBookings]=useState<Booking[]>([]),[payments,setPayments]=useState<Payment[]>([]),[query,setQuery]=useState(''),[filter,setFilter]=useState<'all'|'receivable'|'payable'|'unprofitable'>('all');const load=()=>{setStaff(readCurrentStaff());setBookings(read(BOOKINGS,[]));setPayments(read(PAYMENTS,[]))};useEffect(()=>{load();['storage','tn-bookings-updated','happygo-payment-updated','happygo-admin-auth'].forEach(e=>window.addEventListener(e,load));return()=>['storage','tn-bookings-updated','happygo-payment-updated','happygo-admin-auth'].forEach(e=>window.removeEventListener(e,load))},[]);const rows=useMemo(()=>bookings.filter(b=>staff&&canSee(staff,b)).map(b=>{const req=payments.filter(p=>p.bookingId===b.id),latestCustomerPaid=req.reduce((m,p)=>Math.max(m,num(p.customerPaidAmount)),0),sale=bookingRevenue(b),cost=num(b.costPrice),paidSupplier=req.filter(p=>p.status==='paid').reduce((s,p)=>s+num(p.paidAmount||p.amount),0),approvedUnpaid=req.filter(p=>p.status==='approved').reduce((s,p)=>s+num(p.amount),0),requested=req.filter(p=>['pending_director','approved','paid'].includes(p.status)).reduce((s,p)=>s+num(p.amount),0),customerDue=Math.max(0,sale-latestCustomerPaid),supplierDue=Math.max(0,cost-paidSupplier),actualProfit=latestCustomerPaid-paidSupplier,expectedProfit=sale-cost,paidCount=req.filter(p=>p.status==='paid').length;return{b,req,sale,cost,customerPaid:latestCustomerPaid,customerDue,paidSupplier,supplierDue,approvedUnpaid,requested,actualProfit,expectedProfit,paidCount}}).filter(r=>{const q=query.trim().toLowerCase();if(q&&!`${r.b.code} ${r.b.customer_name} ${r.b.product} ${r.b.salesStaffName||''}`.toLowerCase().includes(q))return false;if(filter==='receivable'&&r.customerDue<=0)return false;if(filter==='payable'&&r.supplierDue<=0)return false;if(filter==='unprofitable'&&r.expectedProfit>=0)return false;return true}).sort((a,b)=>b.customerDue-a.customerDue||b.supplierDue-a.supplierDue),[bookings,payments,staff,query,filter]);const totals=useMemo(()=>rows.reduce((a,r)=>({sales:a.sales+r.sale,customerPaid:a.customerPaid+r.customerPaid,receivable:a.receivable+r.customerDue,cost:a.cost+r.cost,supplierPaid:a.supplierPaid+r.paidSupplier,payable:a.payable+r.supplierDue,profit:a.profit+r.expectedProfit}),{sales:0,customerPaid:0,receivable:0,cost:0,supplierPaid:0,payable:0,profit:0}),[rows]);return <section className="admin-panel finance-ledger"><div className="admin-panel-head finance-head"><div><small>SỔ CÔNG NỢ BOOKING</small><h2>Thu khách · Chi nhà cung cấp · Lợi nhuận</h2><p>Dữ liệu được tổng hợp trực tiếp từ giá bán booking, giá vốn và các lệnh chi đã duyệt/đã thanh toán.</p></div></div><div className="finance-kpis"><article><small>DOANH THU BOOKING</small><b>{money(totals.sales)}</b><span>Đã thu {money(totals.customerPaid)}</span></article><article className={totals.receivable>0?'warn':''}><small>CÒN PHẢI THU KHÁCH</small><b>{money(totals.receivable)}</b><span>{rows.filter(r=>r.customerDue>0).length} booking còn công nợ</span></article><article><small>TỔNG GIÁ VỐN</small><b>{money(totals.cost)}</b><span>Đã chi NCC {money(totals.supplierPaid)}</span></article><article className={totals.payable>0?'warn':''}><small>CÒN PHẢI CHI NCC</small><b>{money(totals.payable)}</b><span>{rows.filter(r=>r.supplierDue>0).length} booking còn phải chi</span></article><article className={totals.profit<0?'danger':'good'}><small>LỢI NHUẬN DỰ KIẾN</small><b>{money(totals.profit)}</b><span>Giá bán trừ giá vốn</span></article></div><div className="finance-toolbar"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Tìm mã booking, khách, sản phẩm, Sale..."/><div>{([['all','Tất cả'],['receivable','Còn phải thu'],['payable','Còn phải chi'],['unprofitable','Âm lợi nhuận']] as const).map(([k,l])=><button key={k} className={filter===k?'active':''} onClick={()=>setFilter(k)}>{l}</button>)}</div></div><div className="finance-table-wrap"><table className="finance-table"><thead><tr><th>Booking</th><th>Sale / Khách</th><th>Giá bán</th><th>Khách đã trả</th><th>Còn phải thu</th><th>Giá vốn</th><th>Đã chi NCC</th><th>Còn phải chi</th><th>Lợi nhuận</th><th>Thanh toán</th></tr></thead><tbody>{rows.map(r=><tr key={r.b.id}><td><b>{r.b.code}</b><small>{r.b.product}</small></td><td><b>{r.b.customer_name}</b><small>{r.b.salesStaffName||'Chưa phân Sale'}</small></td><td>{r.sale?money(r.sale):<em>Chưa nhập</em>}</td><td>{money(r.customerPaid)}</td><td className={r.customerDue>0?'debt':''}>{money(r.customerDue)}</td><td>{r.b.costPrice!=null?money(r.cost):<em>Chưa nhập</em>}</td><td>{money(r.paidSupplier)}</td><td className={r.supplierDue>0?'debt':''}>{money(r.supplierDue)}</td><td className={r.expectedProfit<0?'loss':'profit'}>{r.b.costPrice!=null&&r.sale?money(r.expectedProfit):'—'}<small>Thực thu-chi: {money(r.actualProfit)}</small></td><td><span className="finance-pay-status">{r.paidCount} lệnh đã chi</span>{r.approvedUnpaid>0&&<small>Chờ chi {money(r.approvedUnpaid)}</small>}{r.requested===0&&<small>Chưa có đề xuất</small>}</td></tr>)}{!rows.length&&<tr><td colSpan={10} className="finance-empty">Không có booking phù hợp bộ lọc.</td></tr>}</tbody></table></div><div className="finance-footnote">Lưu ý: “Khách đã trả” hiện lấy mức tiền khách đã thanh toán lớn nhất được Sale ghi nhận trong các đề xuất chi của booking, tránh cộng trùng khi một booking có nhiều đề xuất cho nhiều nhà cung cấp.</div></section>}
