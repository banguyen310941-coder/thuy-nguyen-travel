'use client';
import {useEffect,useMemo,useState} from 'react';import {bookingRevenue,canViewCustomer,customerKey,money,readAssignments,readCurrentStaff,type AdminStaff} from '@/components/AdminSalesAccess';
type Booking={id:number;code:string;customer_name:string;phone:string;email?:string|null;product:string;status:string;created_at:string;end_date?:string|null;revenue?:number};type Followup={id:string;customerKey:string;title:string;dueAt:string;status:'open'|'done'};const BOOKINGS='tn_local_bookings_v1',FOLLOWUPS='happygo_crm_followups_v1';function read<T>(k:string,f:T):T{try{return JSON.parse(localStorage.getItem(k)||'')??f}catch{return f}}function monthsSince(v?:string|null){if(!v)return 999;return Math.max(0,(Date.now()-+new Date(v))/2629800000)}
export function AdminCustomerRetention({openCustomer}:{openCustomer?:(key:string)=>void}){const[bookings,setBookings]=useState<Booking[]>([]),[tasks,setTasks]=useState<Followup[]>([]),[staff,setStaff]=useState<AdminStaff|null>(null);useEffect(()=>{const load=()=>{setBookings(read(BOOKINGS,[]));setTasks(read(FOLLOWUPS,[]));setStaff(readCurrentStaff())};load();['storage','tn-bookings-updated','happygo-crm-followups-updated','happygo-admin-auth'].forEach(e=>window.addEventListener(e,load));return()=>['storage','tn-bookings-updated','happygo-crm-followups-updated','happygo-admin-auth'].forEach(e=>window.removeEventListener(e,load))},[]);const rows=useMemo(()=>{if(!staff)return[];const assignments=readAssignments(),map=new Map<string,Booking[]>();bookings.filter(b=>b.status!=='cancelled').forEach(b=>{const key=customerKey(b.phone,b.email);if(!key||key==='email:'||!canViewCustomer(staff,key,assignments))return;map.set(key,[...(map.get(key)||[]),b])});return [...map.entries()].map(([key,list])=>{const sorted=[...list].sort((a,b)=>+new Date(b.end_date||b.created_at)-+new Date(a.end_date||a.created_at)),completed=sorted.filter(b=>b.status==='completed'),last=completed[0]||sorted[0],spent=completed.reduce((s,b)=>s+bookingRevenue(b),0),months=monthsSince(last?.end_date||last?.created_at),openTask=tasks.some(t=>t.customerKey===key&&t.status==='open');return{key,name:last?.customer_name||'Khách hàng',phone:last?.phone||'',count:list.length,completed:completed.length,spent,last,months,openTask}}).filter(x=>x.completed>0).sort((a,b)=>(b.completed-a.completed)||(b.spent-a.spent))},[bookings,tasks,staff]);const returning=rows.filter(x=>x.completed>=2),dormant=rows.filter(x=>x.months>=6&&!x.openTask),vip=rows.filter(x=>x.completed>=3||x.spent>=50000000),revenue=rows.reduce((s,x)=>s+x.spent,0);return <section className="admin-panel"><div className="admin-panel-head"><div><small>CHĂM SÓC SAU CHUYẾN</small><h2>Khách cũ & cơ hội bán lại</h2><p>Nhận diện khách quay lại, khách giá trị cao và khách lâu chưa phát sinh booking mới.</p></div></div><div className="service-kpis"><article><small>KHÁCH ĐÃ HOÀN TẤT</small><b>{rows.length}</b></article><article><small>KHÁCH QUAY LẠI</small><b>{returning.length}</b></article><article><small>KHÁCH VIP</small><b>{vip.length}</b></article><article><small>≥ 6 THÁNG CHƯA QUAY LẠI</small><b>{dormant.length}</b></article></div><div className="crm-auto-box"><div><small>GIÁ TRỊ KHÁCH CŨ</small><b>{money(revenue)}</b><span>Doanh thu từ các booking đã hoàn tất của nhóm khách mà tài khoản này được phép xem.</span></div></div><div className="admin-customer-grid">{rows.slice(0,20).map(x=><article className="admin-customer-card" key={x.key}><div><b>{x.name}</b><a href={`tel:${x.phone.replace(/\D/g,'')}`}>{x.phone||'Không SĐT'}</a><small>{x.last?.product}</small></div><div><b>{x.completed} chuyến hoàn tất</b><small>Tổng giá trị {money(x.spent)}</small><small>{x.months<1?'Vừa đi gần đây':`Khoảng ${Math.floor(x.months)} tháng từ chuyến gần nhất`}</small></div><div>{(x.completed>=3||x.spent>=50000000)&&<span className="status-confirmed">VIP</span>}{x.completed>=2&&<small>↻ Khách quay lại</small>}{x.months>=6&&<small>⚠ Nên tái chăm sóc</small>}{x.openTask&&<small>✓ Đã có lịch chăm sóc</small>}</div>{openCustomer&&<button onClick={()=>openCustomer(x.key)}>Mở CRM</button>}</article>)}</div>{!rows.length&&<div className="admin-empty-state">Chưa có booking hoàn tất để phân tích khách cũ.</div>}</section>}
