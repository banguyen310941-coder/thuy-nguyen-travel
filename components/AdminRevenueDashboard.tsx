'use client';
import {useEffect,useMemo,useState} from 'react';
import {bookingRevenue,customerKey,money,readAssignments,readStaff,type SalesAssignment,type AdminStaff} from '@/components/AdminSalesAccess';
type Booking={id:number;code:string;product:string;customer_name:string;phone:string;email?:string|null;status:string;created_at:string;note?:string|null;revenue?:number;total?:number|string;salesStaffId?:string;salesStaffName?:string;salesAssignedAt?:string};
const BOOKINGS='tn_local_bookings_v1';
function readBookings():Booking[]{try{const x=JSON.parse(localStorage.getItem(BOOKINGS)||'[]');return Array.isArray(x)?x:[]}catch{return[]}}
function attribution(b:Booking,a:Record<string,SalesAssignment>){if(b.salesStaffId)return{staffId:b.salesStaffId,staffName:b.salesStaffName||'',frozen:true};const x=a[customerKey(b.phone,b.email)];return x?{staffId:x.staffId,staffName:x.staffName,frozen:false}:null}
export function AdminRevenueDashboard(){const[items,setItems]=useState<Booking[]>([]);const[staff,setStaff]=useState<AdminStaff[]>([]);const[assignments,setAssignments]=useState<Record<string,SalesAssignment>>({});const[period,setPeriod]=useState('all');const refresh=()=>{setItems(readBookings());setStaff(readStaff().filter(x=>x.role==='sales'||x.department==='sales'));setAssignments(readAssignments())};useEffect(()=>{refresh();window.addEventListener('tn-bookings-updated',refresh);window.addEventListener('tn-staff-updated',refresh);window.addEventListener('happygo-crm-assignment',refresh);return()=>{window.removeEventListener('tn-bookings-updated',refresh);window.removeEventListener('tn-staff-updated',refresh);window.removeEventListener('happygo-crm-assignment',refresh)}},[]);const data=useMemo(()=>{const now=new Date(),filtered=items.filter(b=>{if(!['confirmed','completed'].includes(b.status))return false;if(period==='all')return true;const d=new Date(b.created_at);return period==='month'?d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear():period==='year'?d.getFullYear()===now.getFullYear():true});const ids=new Set(staff.map(s=>s.id));filtered.forEach(b=>{const a=attribution(b,assignments);if(a?.staffId&&!ids.has(a.staffId)){staff.push({id:a.staffId,name:a.staffName||'Sale cũ',role:'sales',status:'inactive'});ids.add(a.staffId)}});const rows=staff.map(s=>{const mine=filtered.filter(b=>attribution(b,assignments)?.staffId===s.id);return{staff:s,count:mine.length,revenue:mine.reduce((n,b)=>n+bookingRevenue(b),0),customers:new Set(mine.map(b=>customerKey(b.phone,b.email))).size,frozen:mine.filter(b=>Boolean(b.salesStaffId)).length}}).sort((a,b)=>b.revenue-a.revenue||b.count-a.count);const revenue=filtered.reduce((n,b)=>n+bookingRevenue(b),0),assignedBookings=filtered.filter(b=>attribution(b,assignments)),assigned=assignedBookings.reduce((n,b)=>n+bookingRevenue(b),0);return{filtered,rows,revenue,assigned,unassigned:Math.max(0,revenue-assigned)}} ,[items,staff,assignments,period]);return <section className="admin-panel revenue-dashboard"><div className="admin-panel-head"><div><h2>Doanh thu & năng lực Sale</h2><p>Booking mới được chốt Sale phụ trách tại thời điểm xác nhận; đổi người phụ trách CRM sau đó không làm thay đổi doanh thu lịch sử.</p></div><select value={period} onChange={e=>setPeriod(e.target.value)}><option value="all">Toàn thời gian</option><option value="month">Tháng này</option><option value="year">Năm nay</option></select></div><div className="revenue-kpis"><article><small>DOANH THU CÔNG TY</small><b>{money(data.revenue)}</b><span>{data.filtered.length} booking chốt</span></article><article><small>DOANH THU ĐÃ GÁN SALE</small><b>{money(data.assigned)}</b><span>Ưu tiên Sale đã đóng băng trên booking</span></article><article><small>CHƯA PHÂN SALE</small><b>{money(data.unassigned)}</b><span>Booking chưa xác định người phụ trách</span></article></div><div className="revenue-table"><div className="revenue-row head"><b>Nhân viên Sale</b><b>Khách</b><b>Đơn chốt</b><b>Doanh thu</b><b>TB / đơn</b></div>{data.rows.map((r,i)=><div className="revenue-row" key={r.staff.id}><span><strong>#{i+1} {r.staff.name}</strong><small>{r.frozen?`${r.frozen} đơn đã khóa Sale`:r.staff.email||'Nhân viên kinh doanh'}</small></span><span>{r.customers}</span><span>{r.count}</span><strong>{money(r.revenue)}</strong><span>{money(r.count?r.revenue/r.count:0)}</span></div>)}</div>{!data.rows.length&&<div className="admin-empty-state"><b>Chưa có nhân viên Sale để thống kê</b></div>}<p className="admin-api-note">Booking cũ chưa có salesStaffId tạm dùng phân công CRM hiện tại. Booking mới khi được xác nhận sẽ khóa Sale phụ trách để giữ lịch sử doanh thu chính xác.</p></section>}
