'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import {resolveCommerce} from '@/components/ProductCommerce';
export type CartItem={id:string;kind:string;product:string;productId?:string;productSlug?:string;unit?:string;unitId?:string;startDate?:string;endDate?:string;adults:number;children:number;rooms:number;priceLabel?:string;sellingPrice?:number;costPrice?:number|null;availableQuantity?:number|null;inventorySource?:string;sellingSource?:string;costSource?:string;priceSourceLabel?:string;priceCalculatedAt?:string;createdAt:string};
const KEY='tn_booking_cart_v1';export function readCart():CartItem[]{try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x:[]}catch{return[]}}export function writeCart(items:CartItem[]){localStorage.setItem(KEY,JSON.stringify(items));window.dispatchEvent(new Event('tn-cart-updated'))}
export function addCartItem(item:Omit<CartItem,'id'|'createdAt'>){const snapshot=resolveCommerce({productId:item.productId,date:item.startDate,manualSelling:item.sellingPrice||item.priceLabel,manualCost:item.costPrice??undefined});if(!snapshot.isAvailable)return null;const requested=Math.max(1,item.rooms||1);if(snapshot.availableQuantity!==null&&requested>snapshot.availableQuantity)return null;const items=readCart();const next:CartItem={...item,sellingPrice:item.sellingPrice||snapshot.sellingPrice||undefined,costPrice:item.costPrice??snapshot.costPrice,availableQuantity:item.availableQuantity??snapshot.availableQuantity,inventorySource:item.inventorySource||snapshot.inventorySource,sellingSource:item.sellingSource||snapshot.sellingSource,costSource:item.costSource||snapshot.costSource,priceSourceLabel:item.priceSourceLabel||snapshot.sourceLabel,priceCalculatedAt:item.priceCalculatedAt||new Date().toISOString(),id:`cart_${Date.now()}_${Math.random().toString(36).slice(2,6)}`,createdAt:new Date().toISOString()};writeCart([...items,next]);return next}
export function BookingCartBadge(){const[count,setCount]=useState(0);useEffect(()=>{const load=()=>setCount(readCart().length);load();window.addEventListener('tn-cart-updated',load);window.addEventListener('storage',load);return()=>{window.removeEventListener('tn-cart-updated',load);window.removeEventListener('storage',load)}},[]);return <Link className="booking-cart-badge" href="/checkout" aria-label={`Giỏ đặt dịch vụ có ${count} mục`}><span>🧳</span><b>Đặt dịch vụ</b>{count>0&&<em>{count}</em>}</Link>}
