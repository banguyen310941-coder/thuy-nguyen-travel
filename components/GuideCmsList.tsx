'use client';

import Link from 'next/link';
import {usePublicGuideArticles,type PublicGuideArticle} from '@/components/usePublicGuideArticles';

export function GuideCmsList({initialItems=[]}:{initialItems?:PublicGuideArticle[]}){const items=usePublicGuideArticles(initialItems);if(!items.length)return null;return <section className="cms-guide-section guide-custom-published"><div className="sub-heading"><div><small>BÀI VIẾT TỪ QUẢN TRỊ</small><h2>Nội dung mới xuất bản</h2><p>Các bài mới tạo trong trang quản trị sẽ xuất hiện tại đây.</p></div></div><div className="guide-grid">{items.map(a=>{const href=`/cam-nang/bai-viet/${encodeURIComponent(a.slug)}`;return <article className="guide-card" key={a.id}><Link href={href} className="guide-image" style={{backgroundImage:`url(${a.cover||'https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1000&q=85'})`}}/><div className="guide-body"><small>{a.category||'Cẩm nang du lịch'} · {a.date||''}</small><h3><Link href={href}>{a.title}</Link></h3><p>{a.excerpt||'Xem nội dung chi tiết và các kinh nghiệm du lịch hữu ích.'}</p><div className="guide-links"><Link href={href}>Đọc bài viết →</Link></div></div></article>})}</div></section>}
