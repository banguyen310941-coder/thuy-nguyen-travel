'use client';
import Link from 'next/link';
import {FormEvent,useEffect,useState} from 'react';
import {useRouter} from 'next/navigation';

export function AffiliateLogin(){const router=useRouter();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{void fetch('/api/affiliate/auth/me',{cache:'no-store'}).then(r=>{if(r.ok)router.replace('/affiliate/dashboard')}).catch(()=>{})},[router]);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');try{const r=await fetch('/api/affiliate/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await r.json().catch(()=>({}));if(!r.ok)return setMsg(data.error||'Không thể đăng nhập.');router.replace('/affiliate/dashboard');router.refresh()}catch{setMsg('Không kết nối được hệ thống CTV.')}finally{setBusy(false)}}return <main className="affiliate-login"><section className="affiliate-login-brand"><div className="affiliate-logo">HG</div><small>HAPPYGO TRAVEL · CỘNG TÁC VIÊN</small><h1>Chia sẻ villa, tăng thu nhập cùng HappyGo</h1><p>Mỗi CTV có mã giới thiệu riêng để chia sẻ sản phẩm, theo dõi booking và nhận hoa hồng rõ ràng trên một dashboard.</p><div className="affiliate-login-points"><span>✓ Link riêng cho từng villa</span><span>✓ Theo dõi đơn và số dư ví</span><span>✓ Hoa hồng minh bạch, dễ đối soát</span></div></section><form className="affiliate-login-card" onSubmit={submit}><small>CỔNG CTV HAPPYGO</small><h2>Đăng nhập</h2><label>Email CTV<input type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="ctv@example.com"/></label><label>Mật khẩu<input type="password" required autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{msg&&<p className="affiliate-message">{msg}</p>}<button disabled={busy}>{busy?'Đang xác thực...':'Đăng nhập Dashboard'}</button><Link href="/">← Về website HappyGo</Link></form></main>}
