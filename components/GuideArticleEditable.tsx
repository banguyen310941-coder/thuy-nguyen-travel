'use client';

import {useEffect,useState} from 'react';
import Link from 'next/link';
import {ContactQuickLink} from '@/components/ContactQuickLink';

type StaticPost={slug:string;title:string;category:string;excerpt:string;image:string;date:string;readTime:string;content:{heading:string;paragraphs:string[]}[]};
type CmsArticle={id:string;title:string;slug:string;category:string;excerpt:string;cover:string;content:string;status:string;date:string};
const happyGoText=(value:string)=>value.replace(/Thúy Nguyên Travel/gi,'HappyGo Travel').replace(/THÚY NGUYÊN TRAVEL/g,'HAPPYGO TRAVEL').replace(/Thúy Nguyên/gi,'HappyGo');
export function GuideArticleEditable({post}:{post:StaticPost}){const [cms,setCms]=useState<CmsArticle|null>(null);useEffect(()=>{const load=()=>{try{const items=JSON.parse(localStorage.getItem('tn_cms_articles_v3')||'[]') as CmsArticle[];setCms(items.find(x=>x.id===`seo_${post.slug}`&&x.status==='published')||null)}catch{setCms(null)}};load();window.addEventListener('tn-articles-updated',load);window.addEventListener('storage',load);return()=>{window.removeEventListener('tn-articles-updated',load);window.removeEventListener('storage',load)}},[post.slug]);const title=happyGoText(cms?.title||post.title);const category=happyGoText(cms?.category||post.category);const excerpt=happyGoText(cms?.excerpt||post.excerpt);const image=cms?.cover||post.image;const date=cms?.date||post.date;const cmsContent=cms?.content?happyGoText(cms.content):'';return <article className="public-article seo-static-article"><header><div className="article-meta"><span>{category}</span><span>{date}</span><span>{post.readTime}</span></div><h1>{title}</h1><p className="article-lead">{excerpt}</p></header><img className="article-cover" src={image} alt={title}/><div className="article-content">{cmsContent?<div dangerouslySetInnerHTML={{__html:cmsContent}}/>:<><p><strong>Tóm tắt:</strong> {excerpt}</p>{post.content.map(s=><section key={s.heading}><h2>{happyGoText(s.heading)}</h2>{s.paragraphs.map((x,i)=><p key={i}>{happyGoText(x)}</p>)}</section>)}</>}</div><div className="article-actions"><Link href="/stay">Xem lưu trú</Link><Link href="/cruises">Xem du thuyền</Link><ContactQuickLink label="Nhận tư vấn"/></div></article>}
