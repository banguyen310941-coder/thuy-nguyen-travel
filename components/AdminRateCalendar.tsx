'use client';

import {useEffect,useMemo,useState} from 'react';

export type RateStatus='available'|'hold'|'soldout';
export type RateRange={id:string;unitId:string;start:string;end:string;price:string;oldPrice:string;quantity:string;minStay:string;status:RateStatus;note:string};
export const RATE_KEY='tn_cms_daily_rates_v1';
const read=():RateRange[]=>{try{const x=JSON.parse(localStorage.getItem(RATE_KEY)||'[]');return Array.isArray(x)?x:[]}catch{return[]}};
const uid=()=>`rate_${Date.now()}_${Math.random().toString(36).slice(2,6)}`;
const blank=(unitId:string):RateRange=>({id:'',unitId,start:'',end:'',price:'',oldPrice:'',quantity:'1',minStay:'1',status:'available',note:''});
export function ratesForUnit(unitId:string){return read().filter(x=>x.unitId===unitId)}
export function rateForDate(unitId:string,date:string){const matches=ratesForUnit(unitId).filter(x=>x.start&&x.end&&date>=x.start&&date<=x.end);return matches.length?matches[matches.length-1]:null}
export function AdminRateCalendar({unitId}:{unitId:string}){const [items,setItems]=useState<RateRange[]>([]);const [form,setForm]=useState<RateRange>(blank(unitId));const [msg,setMsg]=useState('');const load=()=>setItems(ratesForUnit(unitId));useEffect(()=>{load()},[unitId]);const sorted=useMemo(()=>[...items].sort((a,b)=>a.start.localeCompare(b.start)),[items]);function persist(nextForUnit:RateRange[]){try{const all=read().filter(x=>x.unitId!==unitId);localStorage.setItem(RATE_KEY,JSON.stringify([...all,...nextForUnit]));setItems(nextForUnit);window.dispatchEvent(new Event('tn-rates-updated'));return true}catch{setMsg('Không lưu được lịch giá. Bộ nhớ trình duyệt có thể đã đầy.');return false}}function save(){if(!form.start||!form.end){setMsg('Vui lòng chọn từ ngày và đến ngày.');return}if(form.end<form.start){setMsg('Ngày kết thúc phải sau hoặc bằng ngày bắt đầu.');return}if(!form.price.trim()&&form.status==='available'){setMsg('Vui lòng nhập giá bán cho khoảng ngày này.');return}const x={...form,id:form.id||uid(),unitId};const next=items.some(r=>r.id===x.id)?items.map(r=>r.id===x.id?x:r):[...items,x];if(persist(next)){setForm(blank(unitId));setMsg('Đã lưu lịch giá.')}}function edit(r:RateRange){setForm({...r});setMsg('')}function remove(id:string){if(!confirm('Xóa khoảng giá này?'))return;if(persist(items.filter(x=>x.id!==id))&&form.id===id)setForm(blank(unitId))}return <div className="rate-calendar-admin"><div className="rate-calendar-head"><div><b>📅 Lịch giá theo ngày</b><small>Giá riêng theo khoảng ngày sẽ ưu tiên hơn giá ngày thường/cuối tuần/lễ.</small></div><span>{items.length} khoảng giá</span></div><div className="rate-calendar-form"><label>Từ ngày<input type="date" value={form.start} onChange={e=>setForm(f=>({...f,start:e.target.value,end:f.end&&f.end<e.target.value?e.target.value:f.end}))}/></label><label>Đến ngày<input type="date" min={form.start||undefined} value={form.end} onChange={e=>setForm(f=>({...f,end:e.target.value}))}/></label><label>Giá bán/đêm<input inputMode="numeric" value={form.price} onChange={e=>setForm(f=>({...f,price:e.target.value}))} placeholder="4.500.000đ"/></label><label>Giá gốc<input inputMode="numeric" value={form.oldPrice} onChange={e=>setForm(f=>({...f,oldPrice:e.target.value}))} placeholder="5.200.000đ"/></label><label>Số căn còn<input type="number" min="0" value={form.quantity} onChange={e=>setForm(f=>({...f,quantity:e.target.value}))}/></label><label>Ở tối thiểu<input type="number" min="1" value={form.minStay} onChange={e=>setForm(f=>({...f,minStay:e.target.value}))}/></label><label>Trạng thái<select value={form.status} onChange={e=>setForm(f=>({...f,status:e.target.value as RateStatus}))}><option value="available">Mở bán</option><option value="hold">Tạm giữ</option><option value="soldout">Hết phòng</option></select></label><label className="rate-note">Ghi chú<input value={form.note} onChange={e=>setForm(f=>({...f,note:e.target.value}))} placeholder="Flash sale, cuối tuần, lễ..."/></label><div className="rate-form-actions"><button type="button" className="admin-primary" onClick={save}>{form.id?'Cập nhật khoảng giá':'+ Thêm khoảng giá'}</button>{form.id&&<button type="button" onClick={()=>setForm(blank(unitId))}>Hủy sửa</button>}</div></div>{msg&&<small className="rate-message">{msg}</small>}{sorted.length>0&&<div className="rate-range-list">{sorted.map(r=><article key={r.id}><div><b>{r.start} → {r.end}</b><small>{r.note||'Giá theo lịch'}</small></div><strong>{r.status==='soldout'?'Hết phòng':r.status==='hold'?'Tạm giữ':r.price||'Liên hệ'}</strong><span>Còn {r.quantity||'0'} · tối thiểu {r.minStay||'1'} đêm</span><div><button type="button" onClick={()=>edit(r)}>Sửa</button><button type="button" className="danger-action" onClick={()=>remove(r.id)}>Xóa</button></div></article>)}</div>}</div>}
