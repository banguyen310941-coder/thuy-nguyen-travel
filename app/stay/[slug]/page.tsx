import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {stays} from '@/data/catalog';import {StayDetailClient} from '@/components/StayDetailClient';
const SITE='https://happygo.vn';export function generateStaticParams(){return stays.map(stay=>({slug:stay.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=stays.find(x=>x.slug===slug);if(!s)return{title:'Lưu trú'};const url=`${SITE}/luu-tru/${s.slug}`;return{title:s.seoTitle||`${s.name} - ${s.location}`,description:s.seoDescription||s.summary,alternates:{canonical:url},openGraph:{title:s.name,description:s.summary,url,images:[{url:s.image,alt:s.name}],type:'website'},twitter:{card:'summary_large_image',title:s.name,description:s.summary,images:[s.image]}}}
export default async function StayDetailPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=stays.find(x=>x.slug===slug);if(!s)notFound();const url=`${SITE}/luu-tru/${s.slug}`;const schema=[{"@context":"https://schema.org","@type":"LodgingBusiness","name":s.name,"description":s.summary,"image":[s.image],"url":url,"address":{"@type":"PostalAddress","addressLocality":s.location,"addressCountry":"VN"}},{"@context":"https://schema.org","@type":"BreadcrumbList","itemListElement":[{"@type":"ListItem","position":1,"name":"Trang chủ","item":SITE},{"@type":"ListItem","position":2,"name":"Lưu trú","item":`${SITE}/luu-tru`},{"@type":"ListItem","position":3,"name":s.name,"item":url}]}];return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><StayDetailClient base={s}/></>}
