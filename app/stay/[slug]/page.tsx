import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import Link from 'next/link';
import {stays} from '@/data/catalog';
import {BookingInquiry} from '@/components/BookingInquiry';
import {PublishedUnits} from '@/components/PublishedUnits';
import {ContactPhoneInline} from '@/components/ContactPhoneInline';

export function generateStaticParams(){return stays.map(stay=>({slug:stay.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const stay=stays.find(item=>item.slug===slug);if(!stay)return {title:'Lưu trú'};return {title:`${stay.name} - ${stay.location}`,description:stay.summary,alternates:{canonical:`/stay/${stay.slug}`},openGraph:{title:stay.name,description:stay.summary,images:[stay.image],type:'website'}}}

export default async function StayDetailPage({params}:{params:Promise<{slug:string}>}){
 const {slug}=await params;const stay=stays.find(item=>item.slug===slug);if(!stay)notFound();
 const gallery=stay.gallery?.length?stay.gallery:[stay.image,stay.image,stay.image];const isVilla=stay.type==='Villa';
 return <div className="product-detail-v2"><section className="pd-head"><div className="container"><div className="pd-breadcrumb"><Link href="/">Trang chủ</Link><span>›</span><Link href="/stay">Lưu trú</Link><span>›</span><b>{stay.name}</b></div><div className="pd-title"><div><span className="pd-type">{stay.type}</span><h1>{stay.name}</h1><p>📍 {stay.location}</p></div><div className="pd-rating"><span>Rất tốt</span><b>{stay.rating}</b></div></div></div></section><section className="container pd-gallery"><div className="pd-main-img" style={{backgroundImage:`url(${gallery[0]})`}}/><div style={{backgroundImage:`url(${gallery[1]||gallery[0]})`}}/><div style={{backgroundImage:`url(${gallery[2]||gallery[0]})`}}/><a className="pd-gallery-open" href={gallery[0]} target="_blank" rel="noreferrer">📷 Xem ảnh lớn</a></section><section className="container pd-summary-grid"><div className="pd-summary-card"><h2>{isVilla?'Villa nghỉ dưỡng phù hợp gia đình & nhóm':'Lưu trú nghỉ dưỡng cao cấp'}</h2><p>{stay.summary}</p><div className="pd-quick-info"><span>✓ {isVilla?'Nhiều căn villa quản lý riêng':'Nhiều hạng phòng quản lý riêng'}</span><span>✓ Giá ngày thường / cuối tuần / lễ</span><span>✓ Sức chứa & phụ thu riêng từng căn</span><span>✓ Xác nhận tình trạng theo ngày</span></div></div><div className="pd-price-card"><small>GIÁ TỐT NHẤT THEO NGÀY</small><strong>Liên hệ giá</strong><p>Giá thay đổi theo căn/phòng, ngày ở và số khách.</p><a href="#booking">Kiểm tra giá & phòng trống</a><ContactPhoneInline/></div></section><nav className="pd-tabs"><div className="container"><a href="#overview">Tổng quan</a><a href="#rooms">{isVilla?'Từng căn Villa':'Từng hạng phòng'}</a><a href="#amenities">Tiện ích</a><a href="#policy">Chính sách</a><a href="#reviews">Đánh giá</a><a href="#faq">Hỏi đáp</a></div></nav><section className="container pd-body"><main><section id="overview" className="pd-block"><h2>Tổng quan</h2><p>{stay.summary}</p><div className="pd-highlight-grid">{stay.highlights.map(item=><div key={item}>✓ <b>{item}</b></div>)}</div></section><PublishedUnits slug={stay.slug} label={isVilla?'Danh sách từng căn Villa & giá':'Danh sách hạng phòng & giá'}/><section id="rooms" className="pd-block"><div className="pd-block-title"><div><h2>{isVilla?'Hạng Villa tham khảo':'Hạng phòng tham khảo'}</h2><p>Dữ liệu căn/phòng chi tiết phía trên được quản lý riêng trong Admin.</p></div></div><div className="pd-room-list">{(stay.rooms||[isVilla?'Villa tiêu chuẩn':'Phòng tiêu chuẩn','Hạng gia đình']).map((room,i)=><article key={room}><div className="pd-room-photo" style={{backgroundImage:`url(${gallery[i%gallery.length]})`}}/><div><h3>{room}</h3><p>Thông tin chi tiết, mã căn và giá được cập nhật tại khu quản trị.</p></div><div><small>Giá theo ngày</small><strong>Liên hệ</strong><a href="#booking">Chọn</a></div></article>)}</div></section><section id="amenities" className="pd-block"><h2>Tiện ích nổi bật</h2><div className="pd-highlight-grid">{stay.highlights.map(item=><div key={item}>✦ <b>{item}</b></div>)}</div></section><section id="policy" className="pd-block"><h2>Chính sách lưu trú</h2><div className="pd-policy-grid"><div><b>Nhận / trả phòng</b><p>Giờ nhận và trả phòng được xác nhận theo từng sản phẩm/hạng phòng.</p></div><div><b>Trẻ em</b><p>Phụ thu tùy độ tuổi, hạng phòng và số khách thực tế.</p></div><div><b>Đặt cọc</b><p>Mức cọc được xác nhận khi giữ chỗ.</p></div><div><b>Hoàn hủy</b><p>Áp dụng theo gói giá và thời điểm đặt.</p></div></div></section><section id="reviews" className="pd-block"><h2>Đánh giá</h2><div className="pd-review-summary"><b>{stay.rating}/10</b><div><strong>Rất tốt</strong><p>Điểm đánh giá tổng hợp sẽ được quản lý trong CMS.</p></div></div></section><section id="faq" className="pd-block"><h2>Câu hỏi thường gặp</h2><details><summary>Giá từng căn có giống nhau không?</summary><p>Không. Mỗi căn/phòng có thể có giá ngày thường, cuối tuần, lễ và phụ thu riêng.</p></details><details><summary>Có thể chọn đúng mã căn không?</summary><p>Có. Khi Admin nhập mã căn, khách có thể gửi yêu cầu theo đúng căn đó.</p></details></section></main><aside id="booking"><BookingInquiry product={stay.name} kind={isVilla?'villa / resort':'khách sạn'}/></aside></section></div>
}
