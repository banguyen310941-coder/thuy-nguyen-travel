import type {Metadata} from 'next';import Link from 'next/link';import {notFound} from 'next/navigation';import {tours} from '@/data/catalog';import {internationalTours} from '@/data/internationalTours';import {BookingInquiry} from '@/components/BookingInquiry';import {ContactZaloButton} from '@/components/ContactZaloButton';
const SITE='https://banguyen310941-coder.github.io/thuy-nguyen-travel';const allTours=[...tours,...internationalTours];export function generateStaticParams(){return allTours.map(t=>({slug:t.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const tour=allTours.find(t=>t.slug===slug);if(!tour)return{title:'Tour du lịch'};const url=`${SITE}/tours/${tour.slug}`;return{title:tour.seoTitle||`${tour.name} - ${tour.duration}`,description:tour.seoDescription||tour.summary,alternates:{canonical:url},openGraph:{title:tour.name,description:tour.summary,url,images:[tour.image],type:'website'}}}
export default async function TourDetailPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const tour=allTours.find(t=>t.slug===slug);if(!tour)notFound();const gallery=tour.gallery?.length?tour.gallery:[tour.image,tour.image,tour.image];const itinerary=tour.itinerary||[{title:'Ngày 1',morning:'Khởi hành theo lịch xác nhận.'}];const similar=allTours.filter(t=>t.slug!==tour.slug&&t.category===tour.category).slice(0,3);return <div className="tour-rich-page"><section className="tour-rich-head"><div className="container"><div className="tour-breadcrumb"><Link href="/">Trang chủ</Link><span>/</span><Link href="/tours">Tour du lịch</Link><span>/</span><span>{tour.category}</span></div><div className="tour-title-wrap"><div><span className="tour-category">{tour.category}</span><h1>{tour.name}</h1><div className="tour-rating">★ {tour.rating||8.8} <span>{tour.reviewCount||'—'} đánh giá</span></div></div><div className="tour-price-box"><small>Giá từ</small><strong>{tour.priceFrom||'Liên hệ'}</strong><span>/ khách</span></div></div></div></section><section className="container tour-gallery-wrap"><div className="tour-gallery"><div className="tour-gallery-main" style={{backgroundImage:`url(${gallery[0]})`}}/><div style={{backgroundImage:`url(${gallery[1]})`}}/><div style={{backgroundImage:`url(${gallery[2]})`}}/></div></section><section className="container tour-summary-card"><div className="tour-fast-info"><div><span>🕒</span><b>Thời gian</b><small>{tour.duration}</small></div><div><span>✈</span><b>Hãng bay</b><small>{tour.airline||'Theo lịch'}</small></div><div><span>🚌</span><b>Phương tiện</b><small>{tour.transport?.join(' · ')}</small></div><div><span>📍</span><b>Khởi hành</b><small>{tour.departureFrom||'Liên hệ'}</small></div></div>{tour.promotions?.map(p=><div className="tour-promos" key={p}>🎁 {p}</div>)}</section><nav className="tour-tabbar"><div className="container"><a href="#intro">Giới thiệu</a><a href="#itinerary">Lịch trình</a><a href="#price">Bảng giá</a><a href="#policy">Chính sách</a><a href="#faq">Hỏi đáp</a></div></nav><section className="container tour-content-layout"><main><section id="intro" className="tour-content-card"><h2>Giới thiệu tour</h2><p>{tour.summary}</p><h3>Điểm nổi bật</h3><ul className="tour-highlight-list">{tour.highlights?.map(x=><li key={x}>{x}</li>)}</ul></section><section id="itinerary" className="tour-content-card"><h2>Lịch trình tour {tour.duration}</h2><div className="tour-itinerary">{itinerary.map((d,i)=><article key={i}><div className="tour-day-index">{i+1}</div><div><h3>{d.title}</h3>{d.morning&&<p><b>Buổi sáng:</b> {d.morning}</p>}{d.afternoon&&<p><b>Buổi chiều:</b> {d.afternoon}</p>}{d.evening&&<p><b>Buổi tối:</b> {d.evening}</p>}{d.meals&&<small>Bữa ăn: {d.meals}</small>}</div></article>)}</div></section><section id="price" className="tour-content-card"><h2>Bảng giá & dịch vụ</h2><div className="tour-price-table"><div><b>Người lớn</b><strong>{tour.priceFrom||'Liên hệ'}</strong><span>/ khách</span></div><div><b>Trẻ em</b><strong>Liên hệ</strong><span>Theo độ tuổi</span></div><div><b>Phụ thu phòng đơn</b><strong>Liên hệ</strong><span>Theo lịch</span></div></div><div className="tour-two-col"><div><h3>Giá bao gồm</h3><ul>{tour.included?.map(x=><li key={x}>✓ {x}</li>)}</ul></div><div><h3>Không bao gồm</h3><ul>{tour.excluded?.map(x=><li key={x}>– {x}</li>)}</ul></div></div></section><section id="policy" className="tour-content-card"><h2>Chính sách tour</h2><ul>{tour.policies?.map(x=><li key={x}>{x}</li>)}</ul></section><section id="faq" className="tour-content-card"><h2>Câu hỏi thường gặp</h2><div className="tour-faq">{(tour.faq||[{q:'Làm sao nhận giá chính xác?',a:'Gửi ngày dự kiến và số khách để nhân viên xác nhận giá theo lịch.'}]).map(x=><details key={x.q}><summary>{x.q}</summary><p>{x.a}</p></details>)}</div></section>{similar.length>0&&<section className="tour-content-card"><h2>Tour cùng danh mục</h2><div className="tour-similar-grid">{similar.map(t=><Link href={`/tours/${t.slug}`} key={t.slug}><div style={{backgroundImage:`url(${t.image})`}}/><b>{t.name}</b><span>{t.duration} · {t.priceFrom||'Liên hệ'}</span></Link>)}</div></section>}</main><aside className="tour-booking-sticky"><div className="tour-booking-price"><span>Giá từ</span><strong>{tour.priceFrom||'Liên hệ'}</strong></div><BookingInquiry product={tour.name} kind="tour"/><ContactZaloButton className="tour-zalo-fast" label="Zalo tư vấn"/></aside></section></div>}
