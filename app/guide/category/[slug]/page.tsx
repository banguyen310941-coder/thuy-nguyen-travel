import type {Metadata} from 'next';
import Link from 'next/link';
import {notFound} from 'next/navigation';
import {guidePosts} from '@/data/guides';

const categoryMap:Record<string,{name:string;terms:string[];description:string}>={
 'ha-long':{name:'Hạ Long',terms:['hạ long'],description:'Kinh nghiệm du lịch Hạ Long, du thuyền, lịch trình, khách sạn và các gợi ý hữu ích.'},
 'du-thuyen':{name:'Du thuyền',terms:['du thuyền','cabin'],description:'Kinh nghiệm chọn du thuyền, cabin, hành trình và lịch trình nghỉ đêm.'},
 'villa-resort':{name:'Villa & Resort',terms:['villa','resort','oceanami','novaworld','flc'],description:'Kinh nghiệm chọn villa và resort theo khu vực, số khách, hạng phòng và ngân sách.'},
 'nha-trang':{name:'Nha Trang',terms:['nha trang','vinpearl'],description:'Kinh nghiệm du lịch Nha Trang, khách sạn, resort và lịch trình cho gia đình.'},
 'phan-thiet':{name:'Phan Thiết',terms:['phan thiết','phan thiet','novaworld'],description:'Lịch trình và kinh nghiệm nghỉ dưỡng Phan Thiết, NovaWorld và các khu vực lân cận.'},
 'quy-nhon':{name:'Quy Nhơn',terms:['quy nhơn','quy nhon','flc quy'],description:'Kinh nghiệm đặt phòng, nghỉ dưỡng và khám phá Quy Nhơn.'},
 'tour-trung-quoc':{name:'Tour Trung Quốc',terms:['trung quốc','trung quoc','bắc kinh','thượng hải','hàng châu'],description:'Kinh nghiệm đi tour Trung Quốc, lịch trình, chuẩn bị và lựa chọn chương trình phù hợp.'}
};
function matches(p:(typeof guidePosts)[number],terms:string[]){const hay=`${p.category} ${p.title} ${p.excerpt} ${p.keywords.join(' ')}`.toLowerCase();return terms.some(t=>hay.includes(t.toLowerCase()))}
export function generateStaticParams(){return Object.keys(categoryMap).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=categoryMap[slug];if(!c)return {};return {title:`Cẩm nang ${c.name} - Kinh nghiệm du lịch | Thúy Nguyên Travel`,description:c.description,alternates:{canonical:`https://banguyen310941-coder.github.io/thuy-nguyen-travel/guide/category/${slug}`}}}
export default async function GuideCategory({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=categoryMap[slug];if(!category)notFound();const posts=guidePosts.filter(p=>matches(p,category.terms));const other=Object.entries(categoryMap).filter(([s])=>s!==slug);return <div className="guide-portal"><section className="guide-portal-hero"><div className="container"><div className="guide-breadcrumb"><Link href="/">Trang chủ</Link><span>›</span><Link href="/guide">Cẩm nang</Link><span>›</span><b>{category.name}</b></div><div className="guide-hero-grid"><div><span className="guide-kicker">CHUYÊN MỤC CẨM NANG</span><h1>{category.name}</h1><p>{category.description}</p></div></div></div></section><section className="guide-portal-main"><div className="container guide-layout"><aside className="guide-sidebar"><div className="guide-side-card"><h3>Danh mục</h3><Link href="/guide"><span>Tất cả bài viết</span><b>{guidePosts.length}</b></Link><Link className="active" href={`/guide/category/${slug}`}><span>{category.name}</span><b>{posts.length}</b></Link>{other.map(([s,c])=><Link href={`/guide/category/${s}`} key={s}><span>{c.name}</span></Link>)}</div></aside><main className="guide-content"><div className="guide-section-head"><div><small>BÀI VIẾT THEO CHỦ ĐỀ</small><h2>Cẩm nang {category.name}</h2><p>Chỉ hiển thị các bài viết liên quan đến {category.name}.</p></div><span>{posts.length} bài viết</span></div>{posts.length?<div className="guide-card-grid">{posts.map(p=><article className="guide-pro-card" key={p.slug}><Link href={`/guide/${p.slug}`} className="guide-pro-image" style={{backgroundImage:`url(${p.image})`}} aria-label={p.title}><span>{p.category}</span></Link><div className="guide-pro-meta"><small>Thúy Nguyên Travel</small><small>{p.date}</small><small>{p.readTime}</small></div><div className="guide-pro-body"><h3><Link href={`/guide/${p.slug}`}>{p.title}</Link></h3><p>{p.excerpt}</p><Link className="guide-read" href={`/guide/${p.slug}`}>Đọc bài viết →</Link></div></article>)}</div>:<div className="guide-empty"><h3>Chưa có bài viết trong chuyên mục này</h3><p>Bài mới sẽ được cập nhật tại Cẩm nang Thúy Nguyên Travel.</p><Link href="/guide">← Xem tất cả bài viết</Link></div>}</main></div></section></div>}
