'use client';
import Link from 'next/link';import {useEffect,useMemo,useState} from 'react';import {readCart,writeCart,type CartItem} from '@/components/BookingCart';import {formatPhone,useSiteSettings} from '@/components/useSiteSettings';
const BOOKING_KEY='tn_local_bookings_v1',PRODUCTS_KEY='tn_cms_products_v3_units',RATES_KEY='tn_cms_daily_rates_v1';const API_BASE=process.env.NEXT_PUBLIC_API_BASE_URL||'';const normalizePhone=(v:string)=>{const d=v.replace(/\D/g,'');return d.startsWith('84')&&d.length===11?`0${d.slice(2)}`:d};const money=(v?:string)=>{const d=String(v||'').replace(/\D/g,'');return d?Number(d):0};const fmt=(n:number)=>new Intl.NumberFormat('vi-VN').format(n)+'đ';const dateKey=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;const parseYmd=(v:string)=>{const[y,m,d]=v.split('-').map(Number);return new Date(y,m-1,d,12)};const nightsBetween=(s:string,e?:string)=>{if(!s)return 0;if(!e)return 1;const[sy,sm,sd]=s.split('-').map(Number),[ey,em,ed]=e.split('-').map(Number);return Math.max(1,Math.round((Date.UTC(ey,em-1,ed)-Date.UTC(sy,sm-1,sd))/86400000))};const holiday=new Set(['01-01','04-30','05-01','09-02']);const dayKind=(d:Date)=>holiday.has(`${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`)?'holiday':(d.getDay()===5||d.getDay()===6)?'weekend':'weekday';
type Unit={id:string;code?:string;name?:string;weekdayPrice?:string;weekendPrice?:string;holidayPrice?:string};type Product={id?:string;name?:string;slug?:string;units?:Unit[]};type Rate={unitId:string;start:string;end:string;price:string;quantity:string;status:string};type LocalBooking={id:number;code:string;kind:string;product:string;productId?:string;productSlug?:string;unitId?:string;revenue?:number;customer_name:string;phone:string;email:string;start_date:string|null;end_date:string|null;adults:number;children:number;rooms:number;note:string;status:'new';admin_note:string;created_at:string;source:string};
function products(){try{const x=JSON.parse(localStorage.getItem(PRODUCTS_KEY)||'[]');return Array.isArray(x)?x as Product[]:[]}catch{return[]}}function productForItem(i:CartItem){const ps=products();return ps.find(p=>i.productId&&p.id===i.productId)||ps.find(p=>i.productSlug&&p.slug===i.productSlug)||ps.find(p=>p.name===i.product)||ps.find(p=>(p.units||[]).some(u=>i.unitId?u.id===i.unitId:i.unit?.includes(u.code||'')||i.unit?.includes(u.name||'')))||null}function unitForItem(i:CartItem){const p=productForItem(i);return p?(p.units||[]).find(u=>i.unitId?u.id===i.unitId:i.unit?.includes(u.code||'')||i.unit?.includes(u.name||''))||null:null}function rateFor(id:string,date:string){try{const rs=JSON.parse(localStorage.getItem(RATES_KEY)||'[]') as Rate[];const m=rs.filter(r=>r.unitId===id&&date>=r.start&&date<=r.end);return m.length?m[m.length-1]:null}catch{return null}}function fallbackPrice(u:Unit,d:Date){const w=money(u.weekdayPrice),we=money(u.weekendPrice)||w,h=money(u.holidayPrice)||we||w;return dayKind(d)==='holiday'?h:dayKind(d)==='weekend'?we:w}function resolvePrice(i:CartItem){if(i.priceLabel&&money(i.priceLabel))return{label:i.priceLabel,total:0,nights:i.startDate?nightsBetween(i.startDate,i.endDate):0};const u=unitForItem(i);if(!u)return{label:'Liên hệ',total:0,nights:i.startDate?nightsBetween(i.startDate,i.endDate):0};if(!i.startDate){const v=[money(u.weekdayPrice),money(u.weekendPrice),money(u.holidayPrice)].filter(Boolean);return{label:v.length?`Từ ${fmt(Math.min(...v))}`:'Liên hệ',total:0,nights:0}}const nights=nightsBetween(i.startDate,i.endDate),start=parseYmd(i.startDate),prices:number[]=[];for(let x=0;x<nights&&x<31;x++){const d=new Date(start.getFullYear(),start.getMonth(),start.getDate()+x,12),r=rateFor(u.id,dateKey(d)),p=r?(r.status==='available'&&Number(r.quantity||0)>0?money(r.price):0):fallbackPrice(u,d);if(p)prices.push(p)}const total=prices.reduce((s,p)=>s+p,0)*Math.max(1,i.rooms||1);return{label:prices.length?(prices.every(p=>p===prices[0])?`${fmt(prices[0])}/đêm`:`${fmt(Math.min(...prices))} – ${fmt(Math.max(...prices))}/đêm`):'Liên hệ',total,nights}}
export default function CheckoutPage(){const settings=useSiteSettings(),supportPhone=settings.hotline.replace(/\D/g,''),supportLabel=formatPhone(settings.hotline);const[items,setItems]=useState<CartItem[]>([]),[name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[note,setNote]=useState(''),[code,setCode]=useState(''),[error,setError]=useState(''),[sending,setSending]=useState(false),[priceRev,setPriceRev]=useState(0);useEffect(()=>{const load=()=>{setItems(readCart());setPriceRev(x=>x+1)};load();window.addEventListener('tn-rates-updated',load);window.addEventListener('tn-products-updated',load);window.addEventListener('storage',load);return()=>{window.removeEventListener('tn-rates-updated',load);window.removeEventListener('tn-products-updated',load);window.removeEventListener('storage',load)}},[]);const totalPeople=useMemo(()=>items.reduce((s,i)=>s+i.adults+i.children,0),[items]),priced=useMemo(()=>items.map(item=>({item,...resolvePrice(item)})),[items,priceRev]),grandTotal=useMemo(()=>priced.reduce((s,x)=>s+x.total,0),[priced]);function remove(id:string){const n=items.filter(i=>i.id!==id);setItems(n);writeCart(n)}function saveLocal(b:LocalBooking){try{const old=JSON.parse(localStorage.getItem(BOOKING_KEY)||'[]');localStorage.setItem(BOOKING_KEY,JSON.stringify([b,...(Array.isArray(old)?old:[])].slice(0,500)));window.dispatchEvent(new Event('tn-bookings-updated'))}catch{}}
async function submit(){setError('');const ph=normalizePhone(phone);if(name.trim().length<2)return setError('Vui lòng nhập họ tên.');if(!/^0\d{9}$/.test(ph))return setError('Số điện thoại chưa đúng.');if(!items.length)return setError('Giỏ đặt dịch vụ đang trống.');setSending(true);try{const summary=priced.map(({item,label,total,nights},x)=>`${x+1}. ${item.product}${item.unit?` · ${item.unit}`:''}${item.startDate?` · ${item.startDate}${item.endDate?` → ${item.endDate}`:''}`:''} · ${nights||1} đêm · ${item.adults} NL · ${item.children} TE · ${item.rooms} phòng/cabin · Giá: ${label}${total?` · Tạm tính: ${fmt(total)}`:''}`).join('\n'),bookingCode=`HG${new Date().toISOString().slice(2,10).replace(/-/g,'')}${String(Date.now()).slice(-5)}`,single=items.length===1?items[0]:null,p=single?productForItem(single):null,u=single?unitForItem(single):null;const booking:LocalBooking={id:Date.now(),code:bookingCode,kind:single?single.kind:'Nhiều dịch vụ',product:single?single.product:`${items.length} dịch vụ`,productId:single?.productId||p?.id,productSlug:single?.productSlug||p?.slug,unitId:single?.unitId||u?.id,revenue:grandTotal||0,customer_name:name.trim(),phone:ph,email:email.trim(),start_date:items[0]?.startDate||null,end_date:items[0]?.endDate||null,adults:items.reduce((s,i)=>s+i.adults,0),children:items.reduce((s,i)=>s+i.children,0),rooms:items.reduce((s,i)=>s+i.rooms,0),note:`DANH SÁCH DỊCH VỤ:\n${summary}${grandTotal?`\n\nTỔNG TẠM TÍNH: ${fmt(grandTotal)}`:''}${note.trim()?`\n\nGhi chú khách: ${note.trim()}`:''}`,status:'new',admin_note:'',created_at:new Date().toISOString(),source:'checkout-local'};let finalCode=bookingCode;if(API_BASE){try{const payload={...booking,customerName:booking.customer_name,startDate:booking.start_date,endDate:booking.end_date,source:'checkout'};const r=await fetch(`${API_BASE.replace(/\/$/,'')}/api/bookings`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw 0;finalCode=(await r.json()).code||bookingCode}catch{saveLocal(booking)}}else saveLocal(booking);writeCart([]);setItems([]);setCode(finalCode)}catch{setError('Chưa thể lưu yêu cầu. Vui lòng thử lại hoặc gọi hotline.')}finally{setSending(false)}}
if(code)return <div className="checkout-page"><div className="container checkout-success"><div className="checkout-success-icon">✓</div><small>YÊU CẦU ĐÃ ĐƯỢC GHI NHẬN</small><h1>Cảm ơn anh/chị</h1><p>Mã booking của anh/chị là</p><strong>{code}</strong><p>HappyGo Travel sẽ gọi lại để xác nhận giá, tình trạng phòng/cabin và chốt dịch vụ. Bước này chưa thanh toán.</p><div><Link href="/">Tiếp tục xem dịch vụ</Link><a href={`tel:${supportPhone}`}>Gọi {supportLabel}</a></div></div></div>;return <div className="checkout-page"><section className="checkout-hero"><div className="container"><small>HAPPYGO TRAVEL</small><h1>Xác nhận yêu cầu đặt dịch vụ</h1><p>Kiểm tra dịch vụ, giá tạm tính và gửi một lần. Chưa thu tiền ở bước này.</p></div></section><div className="container checkout-layout"><main><div className="checkout-step"><b>1</b><div><h2>Dịch vụ đã chọn</h2><p>{items.length} mục · {totalPeople} khách</p></div></div>{priced.length?priced.map(({item,label,total,nights})=><article className="checkout-item" key={item.id}><div><small>{item.kind}</small><h3>{item.product}</h3>{item.unit&&<p>Hạng/căn/cabin: <b>{item.unit}</b></p>}<p>{item.startDate||'Chưa chọn ngày'}{item.endDate?` → ${item.endDate}`:''}</p><span>{item.adults} người lớn · {item.children} trẻ em · {item.rooms} phòng/cabin</span><div className="checkout-item-price"><small>GIÁ KHÁCH XEM</small><strong>{label}</strong>{total>0&&<b>Tạm tính {nights} đêm × {item.rooms} phòng/căn: {fmt(total)}</b>}</div></div><button type="button" onClick={()=>remove(item.id)}>Xóa</button></article>):<div className="checkout-empty"><b>Chưa có dịch vụ</b><p>Quay lại chọn Tour, Villa, khách sạn hoặc du thuyền.</p><Link href="/">Chọn dịch vụ</Link></div>}{grandTotal>0&&<div className="checkout-grand-total"><span>TỔNG TẠM TÍNH</span><strong>{fmt(grandTotal)}</strong></div>}</main><aside className="checkout-form-card"><div className="checkout-step"><b>2</b><div><h2>Thông tin liên hệ</h2><p>Nhân viên dùng thông tin này để xác nhận booking.</p></div></div><label>Họ và tên<input value={name} onChange={e=>setName(e.target.value)} placeholder="Nguyễn Văn A"/></label><label>Số điện thoại<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder={supportLabel}/></label><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></label><label>Ghi chú<textarea rows={4} value={note} onChange={e=>setNote(e.target.value)}/></label>{error&&<p className="checkout-error">{error}</p>}<button className="checkout-submit" type="button" onClick={submit} disabled={!items.length||sending}>{sending?'Đang gửi...':'Gửi yêu cầu & nhận mã booking'}</button></aside></div></div>}
