import {db,hasDatabase} from '@/lib/db';
import {GUIDE_ARTICLE_STATE_KEY} from '@/lib/guide-publishing-standard';

export type PublicGuideSeo={slug:string;title:string;excerpt:string;category:string;cover:string;seoTitle:string;seoDescription:string;date:string;updatedAt:string};
function envelope(value:unknown){let parsed=value;if(typeof parsed==='string'){try{parsed=JSON.parse(parsed)}catch{return null}}if(!parsed||typeof parsed!=='object'||!('value' in parsed))return null;return (parsed as {value?:unknown}).value}
function mapArticle(item:any):PublicGuideSeo{return{slug:String(item.slug||''),title:String(item.title||''),excerpt:String(item.excerpt||''),category:String(item.category||'Cẩm nang du lịch'),cover:String(item.cover||''),seoTitle:String(item.seoTitle||''),seoDescription:String(item.seoDescription||''),date:String(item.date||item.publishAt||''),updatedAt:String(item.updatedAt||item.publishAt||item.date||'')}}
export async function listPublishedGuideSeo():Promise<PublicGuideSeo[]>{if(!hasDatabase())return[];try{const sql=db();const rows=await sql`select after_data,created_at from audit_logs where entity_type='admin_shared_state' and entity_id=${GUIDE_ARTICLE_STATE_KEY} order by created_at desc,id desc limit 1`;const value=envelope(rows[0]?.after_data);if(!Array.isArray(value))return[];const now=Date.now();return value.filter((item:any)=>(item?.status==='published'||(item?.status==='scheduled'&&item?.publishAt&&+new Date(item.publishAt)<=now))&&item?.cover).map(mapArticle).filter(item=>item.slug&&item.title&&item.cover)}catch{return[]}}
export async function getPublishedGuideSeo(slug:string):Promise<PublicGuideSeo|null>{if(!slug)return null;const items=await listPublishedGuideSeo();return items.find(item=>item.slug===slug)||null}
